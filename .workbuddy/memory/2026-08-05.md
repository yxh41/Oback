# 2026-08-05 工作日志

## 从 GitHub 同步最新进度到本地（13:5x，fast-forward 无冲突）
- 本地落后 origin/main 16 个 commit（ae135bd → b0ca832），`git pull --ff-only` 干净 fast-forward。
- 同步后 `git rev-list HEAD...origin/main` = `0 0`，工作区干净，HEAD = b0ca832。
- 远程 main 已领先于我们昨日一起敲定的全局返回版本，设计演进如下：

### 远程新增/演进内容（关键）
1. **`navPopFallbackApps`（每 App 无动画兜底开关）**：强制 `rightSimplePop`，针对「系统交互转场不渲染」的 App，commit 明确点名 CoolAPK。= 我们昨日给酷安列的修复分支③的正式落地。→ 酷安瞬返根因现已可有开关解法，不必再走 `[diag-navTarget]` 诊断日志（该诊断代码仍在，但可跳过）。
2. **全局返回设计变化（与我版不同）**：`e5d066c` 全局返回 App **左右缘都关掉**（含弹窗 dismiss），把边缘交还系统/App 原生；我版是「关左缘、留右缘 dismiss」。
3. **`globalBackRightSide`**：新增右手侧薄热区触发（全局返回不只左 1/3，也能右缘薄热区触发）。
4. **`b0ca832`**：全局返回补触感反馈（haptic）；设置滑块数值显示合并（去原生 showValue、补 shadowOpacity/cardCornerValue）。
5. `a4d3afa` fix：声明 `ObackNavPopFallbackController` @interface（曾 root class → 编译错误）。
- 注意：远程多个 commit message 重复（同功能多次 push/ amend），历史略乱但不影响代码。

### 结论 / 待办
- 本地现为远程基线，含上述全部演进。
- 后续可选：① 理 `navPopFallbackApps` 用法/设置入口；② 评估远程新版 vs 我版差异的回归风险；③ 如需在新基线继续开发，先读新代码对齐。

## 用户回传 oback_diag(3).log（手动 dump 按钮，2026-08-05 14:1x 分析）
- 内容：仅 2 行，`com.tencent.mqq`(QQ) 与 `com.tigisoftware.Filza`(Filza)，均为 `isAllowed=1 / navPopFallback=0 / leftEdgeExcluded=0`。
- **手动 dump 按钮打的是 App 级状态（bid/isAllowed/各开关/capsuleEffect/state），不含 nav 内部探测**。nav target 探测 `[diag-navTarget]` 只在 `oback_debug.log`（需「调试日志」开关 + 真滑触发 `navPopSystemTargetForNav:`）才有——两个文件不同。
- 矛盾点：用户声称「开了无动画修复开关」，但 dump 里 QQ 的 `navPopFallback=0`（关）。待用户澄清：是否真的对 QQ 勾选、或抓的是关的状态、或开在别处。
- ⚠️ **上一轮结论已推翻（2026-08-05 14:18 回传 oback_debug(3).log 纠正）**：曾推测「QQ 取不到系统 target → 默认 rightSimplePop」。实际日志证明 QQ **有系统 target**——见下。

## 用户回传 oback_debug(3).log（调试日志，2026-08-05 14:18 分析）— 纠正根因
- 抓到 QQ 大量活动：`[diag-left-nav] nav=QQNavigationController`、`shouldBegin=YES (... currentParallaxToView=1)`、`nav-anim -> NTPushPopLib.NTPushPopTransition (original/nil path)`、`nav-intc query (animator=NTPushPopLib.NTPushPopTransition ...)`。
- **整份没有 `[diag-navTarget]` 那一行** —— 该诊断仅在 `navPopSystemTargetForNav:` 返回 nil 时打印；没打印 = QQ 的 `navPopSystemTargetForNav:` 返回**非 nil**（有系统交互转场 target）。`currentParallaxToView=1` 也印证走的是「方案 A 系统原生交互转场（跟手路径）」。
- **真正根因**：QQ 的 nav 用了自研转场库 `NTPushPopLib.NTPushPopTransition`（`Tweak.xm:67` 的 `(original/nil path)` 分支 = 非手势交互时走 `_original` 委托拿到的 App 原生 animator）。Oback 方案 A 把 pan 喂给系统 `handleNavigationTransition:` 做 scrub，但 QQ 的 `NTPushPopTransition` **不是基于 UIViewPropertyAnimator 的可中断转场**，在交互拖动下不渲染 fractionComplete → 页面不动，松手 completeTransition 时它跑一遍完整动画但瞬切 → 表现为「瞬返没动画」。
- 所以「开了无动画修复开关还是瞬返」解释得通：方案 A（系统转场被 QQ 自研接管、scrub 无效）与强制 rightSimplePop（非交互 pop）在用户感知上都是瞬返，`navPopFallbackApps` 对 QQ 是 no-op。
- 日志里 `nav-anim query` 的 `interacting` 全为 0（这批是 push / 点按钮 pop，非手势交互中）；手势交互中（interacting=1）的 pop 走 `Tweak.xm:55` 返回 nil（方案A）。要 100% 坐实「手势 pop 时是否真的返回 nil 交给系统」，可抓一次真正手势滑动触发 `nav-anim -> nil (方案A: 系统原生 pop)` 的日志，但核心结论已定。
- **修法（若要 QQ 跟手）**：对 QQ 走 Oback 自研 `ObackAnimator`（`interruptibleAnimatorForTransition:` 返回 UIViewPropertyAnimator，可被 scrub）驱动 pop，即 `navParallaxEnabled` 自定义视差实验路径（已是阴影渐隐无 reparent 安全方案），需真机验底部空白/导航栏/scrollView 三件套。风险高，除非 QQ 对你特别重要否则不建议动。

## 用户回传 oback_debug(4).log（2026-08-05 14:24 分析，重抓：中间滑动 + 左缘返回）
- `start called, bid=com.tencent.mqq` 确认是 QQ。`attachNavPan: nav=QQNavigationController pans=2 on nav.view (左缘+右缘)`（第23-26行）—— **pans=2 说明 QQ 没在「选全局返回程序」列表**（全局返回 App 应关左缘/只挂右缘或全屏 pan，不该 pans=2）。所以用户说的「中间滑动」本次 Oback 根本没接管（无全屏 pan、无 handleGlobalPan 日志），中间滑动对 Oback 无效。
- **全程 `nav-anim query` 的 `interacting` 全为 0**（第31/36/40/45/49/58/62/71 行等），**没有一次 `interacting=1`，也没有 `nav-anim -> nil (方案A: 系统原生 pop)` 那行**（该行为只在 `operation==Pop && interacting==YES` 时打，Tweak.xm:55-60）。即用户所有滑动要么只到 shouldBegin（左缘显示 indicator）就松手、要么没真正拖动进交互转场。
- 左缘返回（第53-57、66-70行）`shouldBegin=YES (currentParallaxToView=1)` + `indicator shown`，路径通、走方案A，但同样缺 interacting=1 的驱动证据。
- 仍无 `[diag-navTarget]` + 仍反复 `NTPushPopLib.NTPushPopTransition` → **上一份结论不变**：QQ 有系统 target、自研转场不跟手 scrub。
- **本份新信息有限，反而暴露测试配置问题**：①QQ 未在全局返回列表→中间滑动未测；②全程未抓到 interacting=1→无法从真机直接坐实「方案A 喂系统后 scrub 无效」那一瞬间（仅靠代码推理：currentParallaxToView=1 + NTPushPopTransition 非可中断 → scrub 无效 → 瞬返）。
- 给用户下一步：若想补全证据链，需①把 QQ 勾进「选全局返回程序」再测中间滑动；②左缘真正拖动到底（别松太快），抓一次含 `nav-anim -> nil (方案A)` + `beginTransition/updateTransition` 拖动日志。

### 用户确认（oback_debug(3)/(4) 两份都未开全局返回 / 无动画修复开关）
- 用户亲口确认：两份日志抓的时候，QQ **既没勾全局返回、也没勾 navPopFallback**。这把"开关导致瞬返"的假设彻底排除——QQ 的瞬返是**标准左缘返回路径（方案A）的默认表现**，与任何 toggle 无关。
- 代码侧重新核对（Tweak.xm:47-69 + ObackManager.m:1666-1726）：QQ 的 `com.tencent.mqq` 不在 `_navPopShouldDriveSystemNav:` 硬编码（只有微信/com.tencent.xin + MMUI*），`navPopSystemTargetForNav:` 返回非 nil（[diag-navTarget] 未触发）→ 该判定返回 YES → 走方案A、`driveSystemNavPopBeginWithPan:` 把 pan 喂系统 `handleNavigationTransition:`。
- **关键推断（代码+日志）**：oback_debug(4) 全程 `nav-anim query` 的 `interacting` 恒为 0、无一次 `nav-anim -> nil (方案A)`，但 commit 阶段冒 `NTPushPopLib.NTPushPopTransition` → QQ 的 interactive pop **完全自包含在 NTPushPopLib 内，从不走 Oback 的 ObackNavDelegate 交互查询**，Oback 喂的 `handleNavigationTransition:` 对 QQ 自研转场无效 → 拖动不 scrub、松手瞬返。
- 结论：QQ 与微信同属"自定义 nav 不走标准交互转场"，但机制不同——微信=有 machinery 但不渲染（硬编码拉黑）；QQ=自研 NTPushPopLib 整体接管，Oback 的方案A 喂不进去。两者在 Oback 侧都只能非交互返回（瞬返），属 App 固有，非回归。
- 修法仍只有实验路径：对 QQ 走 Oback 自研 `ObackAnimator`（interruptibleAnimatorForTransition: 返回 UIViewPropertyAnimator 可被 scrub），即 navParallaxEnabled 自定义视差链路（已做阴影渐隐无 reparent 安全方案），需真机验底部空白/导航栏/scrollView 三件套。风险高，除非 QQ 对用户特别重要否则不建议动（和微信同样系统级返回本就不跟手）。
- **用户确认「只针对QQ、不影响其他app」的架构保证（已代码核对）**：①路由决策点 `_navPopShouldDriveSystemNav:`(ObackManager.m:1722) 已是按 bundle id 分（微信 `com.tencent.xin` 即例证），QQ 修复=加 `com.tencent.mqq` 分支，爆炸半径仅 QQ；②ObackAnimator/self.interactive machinery 已存在且当前仅右缘 nav pop 在用(1124-1130)，QQ 左缘复用即可；③当前代码**无全局 navParallaxEnabled flag**（仅旧设计文档 overview_nav_coordination.md 提及，实际 ObackManager.m 未引用）→ 不存在开全局开关带崩其他 App 的风险；④QQ 自研 NTPushPopTransition 会被 ObackAnimator 绕过（恰是目的）。其他 App（标准nav方案A/微信/酷安navPopFallback）路径零改动。

## 实现：QQ/TIM 自定义交互 nav pop（commit 74760e8，已 push origin/main）
- **目标**：让 QQ（com.tencent.mqq）与 TIM（com.tencent.tim，QQ 同门、自研转场同样不跟手）走 Oback 自研 `ObackAnimator` 交互 pop，实现跟手（可被 scrub 的 UIViewPropertyAnimator），绕开 NTPushPopLib 不 scrub 的瞬返。
- **爆炸半径**：仅对 `com.tencent.mqq` / `com.tencent.tim` 生效；其他 App 路径一行不动（零回归）。
- **改动文件（5 files, +61/-10，CRLF 校验全过）**：
  1. **ObackManager.h**：新增 `navPopUseObackAnimator` 属性（BOOL）。
  2. **ObackManager.m**：
     - `shouldBegin` nav 分支 + `handleGlobalPan:` Changed 横向确认后：路由 `useOBAnimator = _navPopShouldUseObackAnimator:nav`，覆盖 `currentParallaxToView=NO / rightSimplePop=NO`（左右缘都走自定义转场）。
     - `beginTransition:` 设 `pan.cancelsTouchesInView = (rightSimplePop || navPopUseObackAnimator)`。
     - `triggerTransitionInWindow:` 在右缘分支前新增 QQ/TIM 分支：`popViewControllerAnimated:YES`。
     - 新增 `_navPopShouldUseObackAnimator:`：仅 `com.tencent.mqq`/`com.tencent.tim` 返 YES。
     - `endTransition:` / `abortTransition:` 复位 `navPopUseObackAnimator=NO`（防残留污染下次手势）。
  3. **Tweak.xm**：`ObackNavDelegate.animationControllerForOperation:` 的 `interacting && Pop` 分支内、返 nil 前：若 `navPopUseObackAnimator`，构造 `ObackAnimator`（设 `navPop=YES`）并 `return a`；否则仍返 nil（方案A）。交互控制器 `interactionControllerForAnimationController:` 对 `ObackAnimator` 自动接 `self.interactive`。
  4. **ObackTransition.h**：`ObackAnimator` 加 `navPop` 属性（BOOL，纯平移+阴影渐隐，区别于方案B sheet 缩放飞出）。
  5. **ObackTransition.m**：`OBApplyParallax` 加 `navPop` 参数（`fromScale= navPop?1.0:(1.0-0.08*percent)`）；`interruptibleAnimatorForTransition:`/`forceFinishIfNeeded` 两处调用传 `self.navPop`；终态缩放 `navPop?1.0:0.92`。
- **复用既有 machinery（未改）**：`updateTransition:` 的 `else(!currentParallaxToView && !rightSimplePop)` 分支天然走 `self.interactive updateWithPercent:p` → 覆盖 QQ/TIM scrub；`endTransition:` 的 `_transitionTriggered` 收尾段 `commit?finish:cancel` 自动收尾，无需改。
- **CI**：push 后自动出 `0.1.0+74760e8`，远程 main = 74760e8。
- **待真机验证（仅在 QQ/TIM 验三件套）**：①底部空白；②导航栏损坏；③scrollView 错位；并确认左缘/右缘/（若勾全局返回）中间滑动均跟手、其他 App 零变化。若有问题需回退或调整（TIM 已主动一并处理，符合「到时也一起处理」）。

## 修复：QQ/TIM 左缘仍瞬闪不跟手（commit ae5d15f，已 push origin/main）
- **现象（真机）**：`74760e8` 装包后右缘跟手、左缘仍瞬闪。代码路由左右缘对称（shouldBegin 覆盖对左右都一样），故非路由 bug，而是**手势抢占**。
- **根因**：右缘能跟手是因为 `_obLinkRightEdgeOpponentPansIfStale`（shouldBegin 懒补链）+ `_obLinkRightEdgeOpponentPansInWindow`（链接时机）把 QQ 的右缘自定义手势压成「失败于我们的右缘 pan」→ Oback 独占。左缘**缺这环**：QQ 左缘 `NTPushPopLib` 自定义手势不与系统 `interactivePopGestureRecognizer` 共用（禁用后者压不住它），左缘起滑时我们的 pan 与 QQ 左缘手势同时开火、抢走转场 → Oback 的 `ObackAnimator` 被覆盖 → 瞬闪。
- **修法（镜像右缘）**：新增 `_obLinkLeftEdgeOpponentPansInWindow:` / `_obLinkLeftEdgeOpponentPansIfStale:`（逻辑同右缘，针对 `UIRectEdgeLeft`），仅对 QQ/TIM（`navPopUseObackAnimator=YES`）生效：
  - `shouldBegin` 左缘且 `navPopUseObackAnimator` 时调 `_obLinkLeftEdgeOpponentPansIfStale:`（懒补链，2s 节流，抓晚到手势）；
  - `_linkNavPopGesturesInWindow:` 链接时机也调（门控 `[self _navPopShouldUseObackAnimator:nil]`），与右缘对称。
  - 压制只在我们左缘 pan 真正 begin 时生效，中间滑动不受影响（QQ 原手势保留）→ 零回归。
- **改动**：仅 ObackManager.m，+61/-1，CRLF 校验 0 LF-only。CI 出 `0.1.0+ae5d15f`。
- **验证**：装包后 QQ/TIM 左缘应跟手；若仍瞬闪，开调试日志抓左缘，看是否有 `trigger: QQ/TIM nav pop 自定义 ObackAnimator`（有=进了 Oback 转场但仍被覆盖，需更强压制；无=左缘未进 Oback 转场，需查 QQ 左缘手势身份）。

## 修复：QQ/TIM 全屏返回（聊天任意位置）瞬返（commit 344693d，已 push origin/main）
- **现象（真机）**：`ae5d15f` 后左缘/右缘跟手，但 QQ 聊天界面"任意位置都能触发"的全屏返回手势仍瞬返。
- **根因**：Oback 全屏 pan(`panG`，window 级 `UIPanGestureRecognizer`，`kGlobalPanKey` 标记、`delegate==self`)的 `_globalPanShouldBegin:` 默认只放「左 1/3 热区」起滑才允许接管（ObackManager.m:1004-1006）。QQ 全屏返回是「任意位置」触发，用户从中间/右侧滑不在热区 → `panG` 不 begin → QQ 自带 `NTPushPopLib` 全屏手势(`plain UIPanGestureRecognizer`)抢先接管转场 → 瞬返。路由本身没问题（`handleGlobalPan:` 对 QQ 同样走 `useOBAnimator`，ObackAnimator 跟手）。
- **修法（双管齐下，仅 QQ/TIM）**：
  1. **取消热区限制**：`_globalPanShouldBegin:` 内 `if ([self _navPopShouldUseObackAnimator:nil])` 分支跳过热区判定，QQ/TIM 任意位置起滑都允许 `panG` 识别；是否真接管仍由 `handleGlobalPan:` 横向占优(>1.69x)判定（纵向/非横向自动 `_cancelGlobalPan:` 交还）。其他 App 保留左 1/3 / 右 1/4 薄热区（零回归）。
  2. **全屏对手压制（镜像左缘）**：新增 `_obLinkFullScreenOpponentPansInWindow:`（找 `panG` = window.gestureRecognizers 里 `delegate==self` 且 `kGlobalPanKey` 标记者）+ `_obLinkFullScreenOpponentPansIfStale:`（2s 节流懒补链）。遍历窗口内所有 `UIPanGestureRecognizer`：`delegate==self` 跳过（防自引用）、`UIScrollViewPanGestureRecognizer` 跳过（防吞聊天列表滚动），其余（QQ 全屏返回手势）`requireGestureRecognizerToFail:panG` → Oback 独占全屏返回、QQ 全屏手势让步（单向、无死锁）。调用点：①`_linkNavPopGesturesInWindow:` QQ/TIM 分支（链接时机，与左缘对称）；②`_globalPanShouldBegin:` 返回 YES 前（懒补链，治进会话后懒加载的晚到全屏手势）。
- **改动**：仅 ObackManager.m（+57/-5，含两个新方法 + 两处调用 + 热区放开），CRLF 校验 0 LF-only。CI 出 `0.1.0+344693d`。
- **验证**：装包后 QQ/TIM 聊天界面任意位置横向滑应跟手（走 ObackAnimator 阴影渐隐）。重点验：①底部空白；②导航栏损坏；③scrollView 错位；④聊天列表纵向滚动是否顺（已跳过 scrollView pan，理论不受压）；⑤其他 App 全局返回热区不变、零回归。若 QQ 全屏手势仍抢（压不到），开「双返回诊断」开关抓 `diag[全屏链接]` 看命中对手 pan 类名，再针对性补识别。

## 现象（344693d 真机）：QQ 全屏返回仍瞬返（Oback 全局手势开了也一样）
- **用户反馈（2026-08-05 下午）**：QQ 自带全屏返回瞬返；且**开了 Oback 全局返回后，QQ 里 Oback 的全屏手势也瞬返**（左/右缘跟手正常）。说明全屏(中间)这条路径在 QQ 里根本没接管成功，而非单纯被抢。
- **当前假设（待日志坐实）**：QQ 聊天的全屏返回手势极可能挂在聊天 scroll 上的 `UIScrollViewPanGestureRecognizer`（或该 scroll 的自定义 pan 子类），而 `_obLinkFullScreenOpponentPansInWindow:` 链接压制时**显式 `if (scrollPanCls) return` 跳过了 scroll 类 pan** → 它从未被要求 `requireToFail panG` → QQ 全屏手势始终抢赢 → Oback 的 panG 要么根本不 begin、要么 begin 后被覆盖 → 瞬返。左/右缘能跟手正因为它俩是 `UIScreenEdgePanGestureRecognizer`（非 scroll 类，被正常链接压制）。
- **诊断增强（commit 13341dd，已 push）**：①`diag[全屏链接]` 改为打印**全窗口所有 pan**（含 scroll，用 `★scroll` 标记）+ 其中「非 Oback 非 scroll」对手 pan 单独列出，暴露被隐藏的 scroll 类对手；②`_globalPanShouldBegin:` 返回 YES 前打印 `globalShouldBegin=YES`（含 loc.x / QQ.TIM 标记）；③`handleGlobalPan` Began 打印 `handleGlobalPan Began`；④横向占优确认接管时打印 `_globalDriven=YES (useOBAnimator=)`。CI 出 `0.1.0+13341dd`。
- **取日志方法（给用户）**：装 `0.1.0+13341dd` → 设置开「调试日志」+「双返回诊断」（注意调试日志有 30min 缓存过期）→ 确认 QQ 在全局返回应用列表 → 杀 QQ 重进聊天界面 → 中间横向滑一下（滑一半松）→ Filza 取 `/var/mobile/oback_debug.log` 发回。
- **日志判读**：`globalShouldBegin=YES` 有但 `handleGlobalPan Began` 无 → panG 没 begin（QQ 抢 touch）；`Began` 有但 `_globalDriven=YES` 无 → 横向判定没过；`diag[全屏链接]` 里 `★scroll` 标记的 pan 即疑似 QQ 全屏返回对手 → 下版把它纳入压制（同时规避图片查看器横向滑动误触）。

## 日志实证（oback_debug(5).log，装 13341dd/b395b9d/5cf0d8c 抓）
- **抓到根因**：日志里**毫无 `handleGlobalPan Began`**（用户在聊天/各界面从中间滑多次，panG 从未 begin），证明 QQ 全屏手势抢先接管、panG 没被系统询问；且**`diag[全屏链接]` 从未打印** → 全屏压制从未建立。
- **压制没建立的双重原因**：①链接时机 `_linkNavPopGesturesInWindow:` 第 554 行 `if (kPanKey 为空) 跳过链接` 的 early return，QQ 全局返回 App 无 Oback 边缘 pan（kPanKey 空）→ 613-616 行的全屏压制被跳过；②shouldBegin 时机的全屏压制锁在 `nav.viewControllers.count>1`（旧 1079 行），而右缘压制是无条件懒补链——不对称导致全屏压制两头都没建立。
- **第二个坑（假设坐实）**：`_obLinkFullScreenOpponentPansInWindow:` 原 `if (scrollPanCls) return` 主动跳过 scroll 类 pan；QQ 全屏返回手势极可能挂在聊天 scroll 上，正好被跳过 → 漏压。
- **修复（commit 4a4140a，已 push，CI `0.1.0+4a4140a`）**：①全屏压制**不再跳过 scroll 类 pan**（panG 纵向不 begin→滚动无碍）；②`_globalPanShouldBegin:` 里全屏懒补链移到**无条件**调用（仅 QQ/TIM，与右缘对称）；③attach 挂上 panG 后立即建全屏压制（`_obLinkFullScreenOpponentPansInWindow:`，最早时机）；④`_linkNavPopGesturesInWindow:` 的 early return 对 QQ/TIM 改为「建全屏压制后 return」（覆盖进聊天后懒加载的晚到全屏手势，早于用户滑动）。四处改动仅 ObackManager.m，CRLF 校验 0 LF-only，爆炸半径仅 QQ/TIM。
- **待真机验证（装 0.1.0+4a4140a）**：QQ/TIM 聊天界面任意位置横向滑→应跟手（走 ObackAnimator 阴影渐隐）。重点：①`diag[全屏链接]` 这次应打印且 opp 列表含 `★scroll` 标记的 QQ 全屏返回对手（说明压到了）；②三件套（底部空白/导航栏/scrollView 错位）；③聊天列表纵向滚动是否顺；④图片查看器横向滑切换图片是否被误触返回（已知风险，若误触需加排除当前 VC 判断）；⑤其他 App 零回归。
