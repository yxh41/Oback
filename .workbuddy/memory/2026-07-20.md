# 2026-07-20

## Oback（原 EdgeBack）— OPPO 风格边缘手势返回 tweak（roothide / iOS 16.4.1）
- 在 `C:\Users\Administrator\WorkBuddy\手势` 搭建完整 Theos 工程（Windows 环境生成，未真机编译）。
- 设计：全局注入 UIKit；全屏 `UIPanGestureRecognizer` 挂 keyWindow，边缘判定 + 黑名单 + scrollView 冲突规避；
  nav pop / modal dismiss 走自定义 `UIViewControllerAnimatedTransitioning` + 交互控制器实现 OPPO 视差
  （当前页平移、上一页阴影+缩放+遮罩）。
- 转发 delegate 保留 App 原逻辑；带 PreferenceLoader 设置面板（触发宽度/左右开关/视差/缩放/遮罩/时长/黑名单/触感）。
- 用户选定：左右边缘都返回 + OPPO 视差 + 全局+内置黑名单 + 完整工程含设置面板。
- 参考：Flashback、FDFullscreenPopGesture、TZScrollViewPopGesture、PanCake、roothide/Developer。
- 待办：需在 Mac + roothide Theos 上 `make package` 真机验证；重点调优点见 README（cancel 视图恢复、原生手势冲突、present 样式）。
- 命名澄清（用户纠正）：**插件名 = Oback**（显示名/包名/类名/文件统一为 Oback：前缀 `Oback*`、函数 `OBApplyParallax`、包名 `com.zlhkf.oback`、设置包 `ObackSettings.bundle`）；**开发者(作者)名 = zlhkf**（control 的 Author/Maintainer 改为 zlhkf，包名开发者前缀用 zlhkf）。上一轮误把插件名改成 zlhkf 已还原。
- 推 GitHub：本地 `git init` 已提交（commit 含源码+layout+`.gitignore`+`.github/workflows/build.yml`）。本环境 Windows 无法编译 iOS tweak，故用 **GitHub Actions CI**（macOS runner + `xzr46770661/action-build-theos` action）在 push 后自动 `make package THEOS_PACKAGE_SCHEME=roothide` 并上传 .deb 产物。`.workbuddy/` 已加入 .gitignore 不进仓库。
- **环境网络限制（重要）**：本对话运行沙箱**HTTPS(443) 到 github.com 被挡**（即使 `dangerouslyDisableSandbox:true` 仍 `Connection was reset`/超时）。但 **SSH(22) 到 github.com 通畅**：密钥 `~/.ssh/id_ed25519_github` 已注册 GitHub 账号 yxh41（`ssh -T git@github.com` 返回 `Hi yxh41!`）。故 git 推送**必须用 SSH remote**（`git@github.com:yxh41/Oback.git`），不能用品 HTTPS。参考：用户另一 tweak `RunningDotIndicator` 的 remote 也是 SSH `git@github.com:yxh41/RunningDotIndicator.git`，所以「他能直接推」。
- **Oback 已成功推上 GitHub（SSH）**：remote 改为 `git@github.com:yxh41/Oback.git`，`git push -u origin main` 成功（main 跟踪 origin/main）。远端原脚手架 README 与我们的冲突，已用 merge(`--allow-unrelated-histories`) 保留我们的 Oback README。push 后 GitHub Actions 自动编译 roothide .deb（`.github/workflows/build.yml`，action `xzr46770661/action-build-theos`），产物在 Actions Artifacts（oback-deb）。
- **重命名**：用户要求插件名改为 **Oback**。已将全部标识符 `EdgeBack*`→`Oback*`、包名 `com.felix.edgeback`→`com.felix.oback`、函数 `EBApplyParallax`→`OBApplyParallax`，并重命名源文件（ObackManager/Transition/Preferences.*、Oback.plist）与设置包目录 `ObackSettings.bundle`。Grep 已确认无残留。
- **状态复核（SSH ls-remote 核实）**：远端 HEAD=`db7ab6d` 与本地 `main` 一致，工作区干净。CI 工作流 `build.yml` 配置正确（`xzr46770661/action-build-theos@main` + `THEOS_PACKAGE_SCHEME=roothide` + 上传 `oback-deb`）。**沙箱无法查 Actions 进度（HTTPS 443 挡，仅 SSH 22 通）**，需用户在 `https://github.com/yxh41/Oback/actions` 自查编译结果并下载 `oback-deb`。可选后续：①README 加「GitHub Actions 自动编译/下载 deb」小节；②静态过源码查编译错误（Windows 不能 make）；③等 CI 日志。
- **白名单模式（用户新增需求，已实现本地未 push）**：`Oback.plist` 保持 `com.apple.UIKit` 全局注入（filter 静态、无法运行时按设置决定是否注入某 App，故只能注入后运行时判断）。`ObackPreferences` 新增 `+isAllowed` 统一判断 + 私有 `_bundleId:inList:`（逗号/换行分隔）；`whitelistMode`（未设置默认当 YES）+ `whitelistRaw`。白名单模式下仅白名单 App 生效、空列表=全不生效；关 `whitelistMode` 回到原「全局+黑名单排除」。`ObackManager.m:51` 调用 `isAllowed` 替换 `isBlacklisted`；`Root.plist` 新增「生效范围」分组（PSSwitch `whitelistMode` 默认 true + PSTextField `whitelistRaw`）。静态自检通过（grep 调用点干净、plistlib 校验 14 entries）。已 commit 并 push（远端 HEAD=5043f70），GitHub Actions 自动触发编含白名单模式的新 deb，用户去 `https://github.com/yxh41/Oback/actions` 自查编译结果并下载 `oback-deb`。README 仍未同步「默认仅白名单生效」（用户选直接 push，未改 README，后续可补）。
- **迁移核对（新机）**：用户把 Oback 工程从旧电脑（`C:\Users\Administrator\WorkBuddy\手势`）手动搬至新机 workspace `C:\Users\hxy24\WorkBuddy\手势`，让我检查覆盖位置。结论：结构完整自洽，覆盖位置**正确**——Makefile `Oback_FILES` 引用的 4 个源文件（Tweak.xm/ObackManager.m/ObackTransition.m/ObackPreferences.m）齐全且一一对应；头文件 import 链闭合（`ObackAnimator`/`ObackInteractiveTransition`/`ObackParams` 均声明于 ObackTransition.h）；`ObackSettings.bundle/{Info,Root}.plist` 完整；`.github/workflows/build.yml` 无硬编码错误路径；`.gitignore` 已排除 `.workbuddy/`。仍受限于 Windows 环境，需 macOS+roothide Theos 真机 `make package` 验证（CI 已配置）。
