# 2026-08-06 Oback 工作日志

## QQ/TIM 全屏返回瞬返根因定位与根治（commit a959693）
- **现象**：装 4a4140a 后抓日志 oback_debug(6).log：左/右缘跟手，但 QQ 聊天界面「任意位置」全屏返回仍瞬返。
- **日志实证**：`_globalPanShouldBegin:` 只打了第 1072 行 `globalShouldBegin: QQ/TIM 全屏 pan 不限热区`，**从没打第 1095 行 `globalShouldBegin=YES`**，也从未出现 `handleGlobalPan Began` → 证明 panG 从未 begin（被 QQ 抢 touch 只是表象，真因是 shouldBegin 实际 return NO）。
- **根因**：全屏 panG 挂 window 上、**无 `kObackNavKey` 关联**（该关联只在 `attachNavPan:` 边缘手势里 `setAssociatedObject`，535 行）；`_globalPanShouldBegin:` 只能靠 `topMost:` 解析 nav。QQ 聊天 VC 嵌自定义容器，`top.navigationController` 返回 **nil** → `if(nav && count>1)` 永不成立 → `return NO`。左/右缘跟手恰因它们有 `kObackNavKey` 关联 QQNavigationController，不依赖 topMost。
- **修复（a959693）**：QQ/TIM 下当 `topMost` 解析不到 nav 时，兜底遍历同 window 的 Oback 边缘 pan（已正确关联 nav）借用其 nav，并 `setAssociatedObject(pan,kObackNavKey,nav)` 写回 panG（供 `handleGlobalPan` 1158 行取 nav 驱动 pop）。仅 QQ/TIM 分支生效，其他 App 走原 topMost → 零回归。借到的 nav=QQNavigationController，栈深动态：列表=1 不接管、聊天=2 接管（符合预期）。
- **CI**：已 push `a959693`，出 `0.1.0+a959693`。
- **待真机验证**：装包后 QQ/TIM 聊天界面中间任意位置横滑应跟手；重点验三件套（底部空白/导航栏损坏/scrollView 错位）+ 聊天列表纵向滚动是否顺 + 图片查看器横向滑是否误触返回（若有，加「当前 VC 是图片查看器则排除」判断）。

## (7)/(8) 日志对比分析（用户给的"QQ全局ON / TIM全局OFF"）
- 两日志均 2026-07-24 抓取，**关键发现：两份都完全没有全屏 pan(panG) 的任何痕迹**——无 `attached 全局返回全屏 pan to window`、无 `globalShouldBegin`、无 `handleGlobalPan Began`、无 a959693 借 nav 日志。属 **344693d 全屏特性之前的预特性包**。
- (7) QQ 全局返回ON：仅边缘手势有效（line 78 `edge=左 nav.childCount=2 YES`）。中间/右侧触摸（line 77/80/85/92 `x=289/131/196/228 不在边缘`）被边缘 pan 拒，**无 panG 接手**→证明装的是全屏特性尚未就绪的构建，全局返回开关未真正挂上 panG。
- (8) TIM 全局返回OFF：仅边缘手势（line 38+ `edge=左/右 YES`），无 panG，符合预期。末行 `start skipped (extension process, bid=com.tencent.xin.sharetimeline)` 是微信分享扩展，无关。
- **结论修正（用户确认装的是 `0.1.0+ecb1352`，即含 a959693 nav 修复的最新包）**：(7)/(8) 是陈旧日志，其内容（全无 globalShouldBegin/借 nav 日志）证明来自 `344693d` 全屏特性之前的构建，**不能代表 ecb1352**。用户在 ecb1352 上仍感瞬返，但 (7) 不是 ecb1352 的日志，无法判断 a959693 是否真生效。
- **(9) ecb1352 实机日志定位真因（已修复）**：用户装 `0.1.0+ecb1352`（含 a959693）。(9) 证实 panG 已 attach、`globalShouldBegin: QQ/TIM 全屏 pan 不限热区` 多次打印，但**永无** `解析/借用 nav` 行与 `globalShouldBegin=YES` / `handleGlobalPan Began` → panG 从不 begin → QQ 原生 NTPushPopLib 瞬返。根因：`_attachNavPanToNav` 全局返回开启时 `if(isGlobalBackEnabled) continue`(ObackManager.m:526) 跳过挂 nav.view 边缘 pan，a959693 借 nav 靠这些边缘 pan 的 `kObackNavKey`——边缘 pan 缺席→借不到→nav nil→shouldBegin NO。修复：借边缘 pan 落空时兜底 `_topNavControllerInWindow:` 递归 VC 树解析顶层 UINavigationController（已写入 ObackManager.m）。待 rebuild + 真机验三件套（底部空白/导航栏损坏/scrollView错位）+ 图片查看器横向滑误触。

- **已推送 1022bca（2026-08-06）**：上条修复已 commit 并 push 到 main，CI 出包 `0.1.0+1022bca`。即全局返回开启时借边缘 pan 落空 → 改走 `_topNavControllerInWindow:` 递归 VC 树解析顶层 nav。待用户装 `0.1.0+1022bca` 按 (9) 流程重测：新日志应出现 `global QQ/TIM: 解析 nav=QQNavigationController count=2` + `globalShouldBegin=YES` + `handleGlobalPan Began`，且 QQ 聊天中间横滑跟手。若仍瞬返，重新抓日志看 nav 是否解析到。

- **(10) 0.1.0+1022bca 实机：胶囊出但无返回（根因②定位+修复）**：oback_debug(10).log 证实 1022bca 的 nav 解析修复生效——`globalShouldBegin=YES (有nav pop=2)`、`handleGlobalPan Began`、`_globalDriven=YES（接管转场）`、`indicator shown(edge=左)` 均出现（胶囊正常）。但 `beginTransition: top=DrawerViewController` 且 `endTransition commit=1` 后 `oback-intc finish (animator=0x0)`：交互转场动画器全程 nil，页面不动。根因②（主因）：`triggerTransitionInWindow:withPan:` 取 nav 只对 `kPanKindKey==@"nav"` 读 `kObackNavKey`，QQ/TIM 全屏 pan(kind=@"window") 走 `topMost:` 拿到 DrawerViewController→nav=nil→`popViewControllerAnimated:` 从未调用（日志无 `pop nav` 行）→ObackNavDelegate.animationControllerForOperation 不被调→`interactive.animator`/`currentAnimator` 始终 nil（`animator=0x0` 是下游症状，非独立 bug；`interactive.animator` 本身确实从未被赋值，但它的赋值点在 animationControllerForOperation 里，pop 不触发就永远 nil）。修复：triggerTransition 优先读 `objc_getAssociatedObject(pan, kObackNavKey)`（shouldBegin 已为 QQ/TIM 全屏 pan 写入 QQNavigationController, pop=2），再退回原 nav-kind/topMost 兜底。非 QQ/TIM 全屏 pan 不写 kObackNavKey→走原路径，零回归。待 rebuild 推 main 后装 `0.1.0+<新sha>` 重测：日志应出现 `pop nav (childCount=2) delegateAfter=... isOback=1` + `nav-anim -> ObackAnimator` + `startInteractiveTransition (animator 非nil)` + `interruptible built pa=...`，且 QQ 聊天中间横滑跟手返回。

- **(11) 0.1.0+1e47533 实机回归：聊天列表无法上下滑（cancelsTouchesInView 残留）**：1e47533 让 QQ 聊天中间横滑跟手返回后，用户反馈聊天列表纵向滑失效（1022bca 也已埋此问题但当时未测纵向，用户说"上个版本就会了"）。根因：`beginTransition` 在确认横向意图（`_globalDriven=YES`→1203 调 beginTransition）后设 `pan.cancelsTouchesInView=YES`（QQ/TIM 路径 navPopUseObackAnimator=YES），但**手势取消/结束从未重置回 NO**→任一次横向返回后 panG.cancelsTouchesInView 永久 YES；QQ/TIM `shouldBegin` 任意位置允许 begin，纵向滑动时 panG 识别瞬间吞掉 touch→scrollView 收不到首发 touch→列表滚不动。修复（8501098）：①`handleGlobalPan` Began 分支每轮确定性 `pan.cancelsTouchesInView = NO`（纵向滑动 touch 正常派发给列表）；②`beginTransition` 内先重置 NO、仅 `rightSimplePop||navPopUseObackAnimator` 时设 YES（横向接管后吞后续 touch 防误触上一页元素）。已推送 8501098，CI 出 `0.1.0+8501098`。验证：聊天列表上下滑正常 **且** 中间横滑仍跟手返回（两者都要 OK）。

- **(12) 0.1.0+ac1f42a 修复聊天框纵滚被吞 + 图片查看器横滑误触返回（scrollView 反向优先）**：用户装 8501098 后：①聊天框（对话内容）仍无法上下滑；②图片查看器横滑误触返回；③聊天列表左边侧边栏触发不爽（待澄清）。根因：8501098 只修 `cancelsTouchesInView` 残留，更深缺陷在 `_obLinkFullScreenOpponentPansInWindow:`(757 行)——它对**所有**对手 pan（含 `UIScrollViewPanGestureRecognizer`）建 `[g requireGestureRecognizerToFail:globalPan]`，即 scrollView 的 pan 必须等全屏 panG 失败后才滚。前提「panG 纵向不 begin」对普通热区 App 成立，但 **QQ/TIM 全屏 pan `_globalPanShouldBegin` 任意位置 begin（含纵向）→ 假设崩**：纵向滚 scrollView 被压制（聊天框/列表滚不动）+ 图片查看器横滑 scrollView 等 panG 失败而 panG 横向接管不失败→被抢→误触返回。修复（ac1f42a）：①scrollView pan **反向优先**——`[globalPan requireGestureRecognizerToFail:g]`（scrollView 优先）：纵向滚 scrollView 不失败→panG 不 begin→正常滚；横滑图片查看器 scrollView 不失败→panG 不接管→不误触返回；非 scrollView 的 QQ 全屏返回手势(NTPushPopLib)仍 `[g requireToFail:globalPan]`（Oback 独占返回，瞬返不回归，零回归）。②`_globalPanShouldBegin` QQ/TIM 分支加即时判定：触摸点下 scrollView `contentSize.width > bounds.width*1.2`（横向可滚，如图片查看器 paging）→ 直接 `return NO` 交还 App，双保险且不依赖懒补链时机。已推送 ac1f42a，CI 出 `0.1.0+ac1f42a`。验证：聊天框/列表上下滑正常、图片查看器横滑不误触返回、中间横滑仍跟手返回（三件都要 OK）。问题③（侧边栏）待用户澄清具体表现后再精准修。

- **(13) 0.1.0+81a3e07 修复 QQ 侧边栏抽屉关不掉（与 1/2 同源反向）**：用户澄清"侧边栏只能边缘拉出、关闭只能点按钮不能手势滑回"→ 抽屉关闭手势（QQ 自带非 scrollView pan）被 `_obLinkFullScreenOpponentPansInWindow:` 的 `[抽屉pan requireToFail: panG]`（Oback 优先）压制→抽屉打开后横滑关它被全屏返回抢走。修复：新增 `_qqDrawerOpenInWindow:`（遍历窗口子视图，命中「全屏半透明遮罩 alpha<0.98 且覆盖全屏」或「贴左且 width<屏宽*0.9 的半屏面板」即判抽屉打开）+ `_scanDrawerMask:width:height:` 递归扫描；`_globalPanShouldBegin` QQ/TIM 分支在 return YES 前检测抽屉打开→直接 `return NO` 不接管返回。panG 不 begin→抽屉关闭 pan 的 requireToFail 依赖不被阻塞→抽屉能滑关。已推送 81a3e07，CI 出 `0.1.0+81a3e07`。验证：QQ 抽屉打开后横滑可关闭（不只点按钮）；若仍关不掉说明抽屉浮层特征未命中，需抓「抽屉打开时横滑」日志精准识别抽屉 VC 类名加白名单。

- **(14) 0.1.0+ebef7cd 修复 81a3e07 抽屉检测误命中聊天界面致返回失效（瞬返回归）**：用户装 81a3e07 后反馈"聊天界面中间横滑跟手返回坏了（又瞬返）"。抓 oback_debug(11).log 证实：聊天界面横滑时 shouldBegin 一路走到抽屉检测并反复打印 `globalShouldBegin=NO (QQ 侧边栏抽屉打开, 交还关闭手势)` → 抽屉检测 `_qqDrawerOpenInWindow:` 在**无抽屉的普通聊天界面**误命中 → shouldBegin 提前 return NO 交还 QQ 原生 → QQ 原生 NTPushPopLib 接手瞬返。根因：`_scanDrawerMask:` 第一个条件（全屏 + alpha<0.98 半透明遮罩）把 QQ 聊天界面常驻的全屏半透明视图（背景/输入区遮罩）误判成"抽屉遮罩"。修复（ebef7cd）：①抽屉检测改**双签名**——必须"全屏半透明遮罩 **且** 贴左半屏面板(width<0.98屏宽)"同时出现（`_scanDrawer:` 累积 mask/panel 标志，返回 mask&&panel）；聊天界面只有常驻全屏半透明视图、无贴左面板→不命中→返回恢复。②shouldBegin 调用抽屉检测加触摸点左侧限制 `loc.x < w*0.7`——双保险，聊天界面中间/右侧横滑即使漏判也不拦截。已推送 ebef7cd，CI 出 `0.1.0+ebef7cd`。验证：①QQ 聊天界面中间横滑跟手返回恢复（不再瞬返，日志不应再有 `QQ 侧边栏抽屉打开` 的 NO 行）；②QQ 抽屉打开后左侧(0~0.7w)横滑仍可关（若关不掉，说明 QQ 抽屉 panel 宽度特征未命中或抽屉在独立 overlay window，需再抓「抽屉打开时横滑」日志精准定位 VC 类名，加精确白名单而非浮层特征）。

- **(15) 0.1.0+be33709 根治 ebef7cd 仍未修好的聊天界面返回失效（panel 判定过宽）**：用户装 ebef7cd 后仍反馈"聊天界面中间任意位置横滑跟手返回还是不行"。根因定位：`_scanDrawer:` 的 `panel` 判定（2206 行）`f.origin.x<=2 && width<0.98w` **过宽**——QQ 聊天界面里贴左、非全屏的视图极多（导航栏子视图、各容器），`panel=YES`；叠加聊天界面常驻全屏半透明层 `mask=YES`（alpha 阈值 0.98 仍含背景层）→ 双签名在**普通聊天界面就满足** → 左 0.7 屏横滑被交还 → 返回失效。ebef7cd 只加"双签名+触摸点左 0.7 屏"限制却没收紧 panel 宽度区间，故聊天界面中间横滑（落在左 0.7 屏）仍被误交还。修复（be33709）：①`panel` 收紧到真正抽屉宽度区间 `[0.4w, 0.86w]`（排除近全屏主界面 >=0.98w 与左侧窄控件 <0.4w）；②`mask` 加 alpha 下界 `>=0.05` 排除近全透明背景层（避免被当遮罩）；③`_qqDrawerOpenInWindow:` 加诊断日志 `[drawer-scan] mask=%d panel=%d`——若聊天界面仍误命中，凭此日志精准识别被误判的视图。已推送 be33709，CI 出 `0.1.0+be33709`。验证：①QQ 聊天界面中间任意位置横滑跟手返回（核心，应恢复）；②QQ 抽屉打开后左侧横滑仍可关（若关不掉，再抓「抽屉打开时横滑」日志，我据 `[drawer-scan]`/VC 类名加精确白名单）；③聊天框纵滚、图片查看器横滑避让仍 OK。此 panel/mask 双签名已较稳，但仍属"浮层特征启发式"，若 QQ 某版本抽屉 panel 宽度不在 [0.4,0.86]w 或抽屉在独立 overlay window，可能未命中→届时按抽屉 VC 类名加白名单（不再靠浮层特征）。

- **(16) 0.1.0+e560425 根治 be33709 仍失败的聊天界面返回（QQ 原生全屏返回抢先 pop）**：用户装 be33709 后抓 oback_debug(12).log，聊天界面中间横滑仍不行。日志铁证：shouldBegin=YES + handleGlobalPan Began 都出现（panG 接管了），但每次 pop 时 `nav-anim query (op=2 interacting=0)` → `nav-anim -> NTPushPopLib.NTPushPopTransition (original/nil path)`——Oback 没接管动画，QQ 原生全屏返回抢先 pop 并瞬返；全程无 `_globalDriven=YES`/`beginTransition` 日志。根因：`_obLinkFullScreenOpponentPansInWindow:`(742) 原只通过 `_enumeratePansInView:win` 枚举 **globalPan 所在单个 window** 的 subview 树建立 `[g requireToFail: globalPan]` 压制。QQ 的 NTPushPopLib 全屏返回手势挂在**另一个 overlay UIWindow** 上（不在 globalPan 所在 window 树）→ 枚举不到 → 从未被 requireToFail 压制 → Oback Began 后 QQ 原生不 fail、抢先吞 touch 并 pop（interacting=0 → ObackNavDelegate 返回 nil → 走 NTPushPopLib 瞬返）。修复（e560425）：①对手 pan 枚举从单个 window 扩大到 `[[UIApplication sharedApplication] windows]` 所有可见 window（跨 window 的 requireToFail 依赖对 UIKit 有效，覆盖 overlay window 的 QQ 全屏返回手势）；②去掉 2s 节流（`_obLinkFullScreenOpponentPansIfStale` 直接改调 `_obLinkFullScreenOpponentPansInWindow`），每轮 shouldBegin 重建压制（防 QQ 原生 pan 中途卸载重挂）。已推送 e560425，CI 出 `0.1.0+e560425`。验证：①QQ 聊天界面中间任意位置横滑跟手返回（核心，应恢复；新日志 pop 时 `nav-anim query ... interacting=1` 且 `nav-anim -> ObackAnimator`）；②QQ 抽屉关不掉 / 聊天框纵滚 / 图片查看器横滑避让 仍 OK。若仍抢先：可能 QQ 全屏返回不是 UIPanGestureRecognizer（而是 UIControl/touches 直处理），requireToFail 无效——届时让用户开 `doubleReturnDiag`（OBLog "[全屏链接] 对手 pan" 会列所有 window 的对手 pan 类名）精准定位。

- **(17) 0.1.0+e560425 CI 编译失败修复（112e30e）**：e560425 推上去后 GitHub Actions 编译报 `ObackManager.m:758: [[UIApplication sharedApplication] windows]` deprecated in iOS 15 + 项目开 `-Werror` → 编译 error。根因：跨 window 枚举我直接用了已弃用的 `UIApplication.windows` 属性。修复（112e30e）：主路径改走 `UIApplication.sharedApplication.connectedScenes` → 取 `UIWindowScene.windows`（非弃用）；仅当 `connectedScenes` 为空时用旧 `windows` 属性兜底，并以 `#pragma clang diagnostic push/ignored "-Wdeprecated-declarations"` 包裹压掉 -Werror 告警。CI 重新出包 `0.1.0+112e30e`。注意：今后任何枚举全屏 window 的代码都必须走 UIWindowScene.windows，不得再直接调已弃用 UIApplication.windows。

- **(18) 0.1.0+112e30e 仍失败 + 根因反转：requireToFail 跨 window 对 QQ 原生 pan 不可靠（627022f 已根治）**：用户装 112e30e（含 e560425 跨 window requireToFail）后仍"聊天界面中间横滑返回不行"，抓 oback_debug(13).log + oback_diag(4).log。diag(4) 仅含 `[手动dump]`（bid=com.tencent.mqq isAllowed=1...），**无 `[全屏链接] 对手 pan` 列表** → 当时 doubleReturnDiag 未开。oback_debug(13) 铁证：`globalShouldBegin=YES` + `handleGlobalPan Began` 都正常，但 Began 后紧跟 `nav-anim query (op=2 interacting=0) → NTPushPopLib.NTPushPopTransition (original/nil path)`，全程无 `_globalDriven`/`beginTransition`/`pop nav` → 即 Oback panG 接管后**根本没驱动 pop，是 QQ 原生 NTPushPopLib 抢先调了 popViewControllerAnimated:**（interacting=0，Oback delegate 被顺带查询返回 nil/原生路径瞬返）。drawer-scan 显示 `panel=1 mask=0`（仅 mask=1 才 return NO），故抽屉检测无误杀。根因反转：跨 window `requireToFail` 对 QQ 原生全屏返回手势**不可靠**（原生 pan 挂独立 overlay window 且抢跑，Oback Began 后 QQ 原生仍抢先 pop，Oback 因竞争 self-cancel→interacting=0）。修复（627022f）：**不再依赖 requireToFail，改为 Oback panG Began 接管时直接 `g.enabled=NO` 禁用 QQ 原生全屏返回 pan**（命中条件：①类名含 `PushPop`（NTPushPopLib 系列，跨 window 也抓得到）②或 `g.view` 在 `nav.view` 树上且非 scroll 非 Oback），使其收不到本轮触摸、Oback 独占驱动；手势 Eended/Cancelled/Failed 及 `_cancelGlobalPan` 调 `_restoreQQNativePop` 恢复（enabled=YES）。仅 QQ/TIM（`_navPopShouldUseObackAnimator:`）且有可 pop 的 nav（count>1）时生效，其他 App 零回归。CI 出 `0.1.0+627022f`。验证：①QQ 聊天界面中间任意位置横滑跟手返回（核心；新日志应现 `[QQ 原生 pop 压制] 禁用 <类名>` + `_globalDriven=YES` + `beginTransition` + `nav-anim -> ObackAnimator`）；②聊天框纵滚/图片查看器横滑避让/抽屉关不掉 仍 OK。若仍不对：开 doubleReturnDiag 重抓，看 `[QQ pop 候选]` 列表确认 QQ 原生 pan 真实类名（若不含 PushPop 且不在 nav.view 树，据列表精确匹配其类名补白名单）。
- **经验沉淀**：QQ/TIM 全屏返回这条线历经 多次"修 A 暴露 B"——核心教训是**不要和 QQ 原生 NTPushPopLib 抢同一个 pop 事件的仲裁（requireToFail 跨 window 不稳）**，最稳是 Oback Began 时直接禁用 QQ 原生 pan、独占驱动，结束再恢复。

- **(19) 日志开关「时灵时不灵」诊断 + 修复（977f656，纯日志子系统 bug，与返回逻辑无关）**：用户反映"有时开了没日志、有时关了还有日志"。根因有三：①`debugLogEnabled`（ObackPreferences.m）原本有**独立 5s 缓存**(`OB_DEBUG_LOG_CACHE_TTL=5.0`) 叠加在 `_mergedPrefs` 的 2s 缓存之上 → 改 plist 后 tweak 进程最多 5s 仍读旧值 → 开了延迟出现/关了延迟停止（"时灵时不灵"主因）。②`OBLog`（ObackManager.m）永远 `seekToEndOfFile` 追加，关开关后旧内容留在 `/var/mobile/oback_debug.log` → 抓日志若没先删旧文件，看到历史累积误以为"关了还有日志"。③`OBLog` 第一行即 `if(![ObackPreferences debugLogEnabled]) return;` —— **「双返回诊断」等 diag 日志走 OBLog，故「调试日志」总开关没开时它们一律不写**（这解释了 oback_diag(4) 缺 `[全屏链接] 对手 pan` 列表：当时总开关关着）。修复（977f656）：①`debugLogEnabled` 去掉 5s 独立缓存，直接走 `_mergedPrefs`（统一 2s TTL，与 doubleReturnDiag 行为一致）→ 延迟从 5s 降到 2s；②`OBLog` 加「关→开」翻转分隔标记 `=== 调试日志已开启（以下为开关生效后日志）===`（静态 `_obLogWasOn` 跟踪），明确日志起点边界。CI 出 `0.1.0+977f656`。**用户抓日志规范（根治操作层困惑）**：改「调试日志」开关后**等 2~3 秒**再复现；抓前**先删 `/var/mobile/oback_debug.log`**；要看 `[全屏链接]`/`diag[...]` 列表必须**同时开「调试日志」总开关 + 「双返回诊断」**（后者单独开无效）。

- **(20) 0.1.0+977f656 仍失败**真因定位 + 终极修复（8ae4942）：用户装 977f656（含 627022f 禁用 QQ 原生 pan）后抓 oback_debug(15).log，按"先开双开关删旧日志等 3 秒"规范抓到清晰日志。铁证：`[drawer-scan] mask=0 panel=1`（聊天界面未误杀抽屉判定），`globalShouldBegin=YES` + `handleGlobalPan Began` 正常，且 `[QQ 原生 pop 压制] 禁用` 列表出现了 `RightDragPanGestureRecognizer`/`_UISwipeActionPanGestureRecognizer`/`NTAIOChat.NTAIOQuickReplyGestureRecognizer` 等 15 个 pan，但 `diag[QQ pop 候选]` 列表首行是 **`_UIParallaxTransitionPanGestureRecognizer@UILayoutContainerView`**——即**系统 `interactivePopGestureRecognizer` 本身**，它挂在 `UILayoutContainerView`（= `nav.view` 自己，非 descendant），故 `_suppressQQNativePopForNav` 的 `isDescendantOfView:nav.view` 对其返回 NO、类名也不含 PushPop → **从没被禁用**（禁用列表 15 个里没有它）。更致命的是竞态：`handleGlobalPan` Changed 在首帧检测到 `dy>6`（轻微纵向抖动）即 `_cancelGlobalPan` → `_restoreQQNativePop` 恢复所有被禁 QQ 原生 pan → 其中系统 interactivePop 立即抢回本轮 touch → `popViewControllerAnimated` 走系统原生 parallax（日志 `NTPushPopLib.NTPushPopTransition` = UIKit `_UIParallaxTransition`，即系统原生瞬返）→ 瞬返。成功那次(507)只是首帧恰好纯横向没触发取消，故这次是**竞态 + 漏禁**双重根因。修复（8ae4942，三处）：①`_suppressQQNativePopForNav` 显式禁用 `nav.interactivePopGestureRecognizer`（捕捉 `_UIParallaxTransitionPanGestureRecognizer`，根治漏禁）；②`handleGlobalPan` Changed 对 QQ/TIM 全屏（`_navPopShouldUseObackAnimator`）放宽纵向抖动容忍——横向占优阈值 1.3x→1.5x，**且未达横向占优时不立即取消，仅当纵向明显占优(>12px 且 >1.5x 横向)才交还**（`_cancelGlobalPan` 不再被轻微抖动触发→不会再"误杀→恢复→瞬返"）；非 QQ/TIM 保持原即时取消零回归；③`_globalPanShouldBegin` 开头调 `_restoreQQNativePop` 清扫上一轮残留禁用（保险防永久禁用泄漏）。CI 出 `0.1.0+8ae4942`。验证：①QQ 聊天界面中间任意位置横滑跟手返回（核心；日志应现 `_globalDriven=YES` + `beginTransition` + `nav-anim -> ObackAnimator` 且**不再有中途 `已恢复` 后紧跟的 `NTPushPopLib`**）；②聊天框纵滚（cancelsTouchesInView=NO 仍生效）/图片查看器横滑避让/抽屉关不掉 仍 OK。
- **(20b) 滑动引用/快捷回复被误伤——(15) 日志已实锤**：用户在 8ae4942 之前问过"聊天框滑动引用信息有没有受影响",当时我基于代码推断"很可能受影响"但需实测。现 (15) 日志 `[QQ 原生 pop 压制] 禁用` 列表**直接含** `_UISwipeActionPanGestureRecognizer`（iOS 消息 cell 左滑操作菜单/引用手势）与 `NTAIOChat.NTAIOQuickReplyGestureRecognizer`（QQ 消息快捷回复手势）——二者均挂在 nav.view 树、非 scroll、非 PushPop，被 8ae4942 的 `_suppressQQNativePopForNav` 条件②误禁。即**滑动引用确实失效**（与中间横滑返回共用一轮 touch，被我们的全屏返回抢走/压制）。这是和用户"qq还是有问题"相吻合的回归之一。待用户发基于 dda9475(可靠开关) 的新诊断日志后，维修清单：①中间横滑返回是否真好(8ae4942 目标) ②给 `_UISwipeActionPanGestureRecognizer`/`NTAIOQuickReplyGestureRecognizer` 等"消息 cell 滑动手势"加白名单排除出禁用集合(只排除它们，不动 PushPop+系统 interactivePop 的禁用，保返回不回归)。白名单按类名精确匹配(已锁定确切类名，无需再猜)。
- **(21) 日志开关开/关审计（dda9475，仅日志子系统增强，与返回逻辑无关）**：用户要求"先检查日志开关的开跟关有没有问题，弄完再发 QQ 诊断日志"。审计结论：`debugLogEnabled`/`doubleReturnDiagEnabled` 均统一走 `_mergedPrefs`（TTL 2s，ObackPreferences.m:20/183/193），977f656 已去掉旧 5s 独立缓存，开/关最多 2s 延迟生效、逻辑无 bug；`OBLog` 的"关→开"翻转分隔标记在(977f656)已加。唯一操作层陷阱：append 不清空致"关了还有日志"错觉。增强（dda9475）：`OBLog` 关→开翻转打 `=== 调试日志已开启 ===` 之外，补"开→关"翻转也打 `=== 调试日志已关闭（以下为无效日志/历史）===` 分隔（仅翻转瞬间打一次，不持续写），边界彻底清晰。CI 出 `0.1.0+dda9475`。用户抓日志规范不变：改开关**等 2~3 秒**；抓前**删 `/var/mobile/oback_debug.log`**；看 diag 列表须**同时开「调试日志」+「双返回诊断」**。

- **(22) 0.1.0+47f69f8 修复聊天界面中间横滑返回失效 + 单条消息左滑引用被误杀（日志(17)实锤）**：用户装 dda9475 后抓 oback_debug(17).log（双开关开、删旧日志、等 3 秒），反馈"中间横滑返回 + 单条消息左滑引用 两个还是不行"。铁证：①`[QQ 原生 pop 压制] 禁用` 列表仍含 `_UISwipeActionPanGestureRecognizer`/`NTAIOChat.NTAIOQuickReplyGestureRecognizer`——二者挂 nav.view 树、非 scroll、非 PushPop，被 `_suppressQQNativePopForNav` 条件②(onNav)误禁→**左滑引用失效**；②每次 `handleGlobalPan Began` 同秒紧跟 `已恢复 15 个手势`（即被 cancel，无 `Changed`/`_globalDriven`/`beginTransition`）→ 根因在 `_obLinkFullScreenOpponentPansInWindow` 对**所有** scroll pan 无条件 `[globalPan requireToFail:g]`：聊天是纵表，横滑时 scroll pan(`_UIScrollViewPanGestureRecognizer@NTAIOChatTableView`)先 begin→panG 被强制 fail→取消→返回不走（图片查看器横滑此前正是靠这个让步才不误触，但纵表横滑被一并误伤）。修复（47f69f8，三处）：①新增 `_isQQQuotePan:` 按类名识别 SwipeAction/QuickReply 左滑引用手势；②`_obLink` scroll pan 按能否横向滚动分流——可横向滚动(图片查看器/分页) `[globalPan requireToFail:g]`(panG 让步，不误触返回)；仅纵向滚动(聊天列表) `[g requireToFail:globalPan]`(scroll 让步，横滑由 panG 驱动返回，纵向由 panG 自取消交还 scroll)；③`_suppressQQNativePopForNav` 跳过 `_isQQQuotePan`（不再禁用左滑引用），并在链接处改为 `globalPan requireToFail:引用手势`（panG 让步于引用：左滑引用时它独占、右滑返回时它不 begin→panG 照常驱动）。非 QQ/TIM 全屏路径零改动。CI 出 `0.1.0+47f69f8`。验证：①QQ 聊天界面中间横滑跟手返回（核心）；②单条消息左滑引用恢复；③聊天框纵滚(cancelsTouchesInView=NO 仍生效)/图片查看器横滑避让(另有 shouldBegin `_scrollViewIsHorizontallyScrollableAtPoint` 1290 行双保险)/抽屉关不掉 仍 OK。
**⚠️版本号更正（1475a08）**：47f69f8 推送后 GitHub Actions **编译失败**——`_obLink` 里判定图片查看器横向可滚时误用了 `sv.zoomEnabled`（`UIScrollView` 根本无此属性，缩放状态只能看 `zoomScale`/pinch 手势），项目开 `-Werror` 直接编译 error，`0.1.0+47f69f8` 包**未出成**（所以让用户装 47f69f8 是错的）。修复（1475a08）：去掉 `sv.zoomEnabled` 子条件，横向可滚判定保留 `pagingEnabled || contentSize.width > bounds.width + 1`（图片查看器靠 pagingEnabled 命中）。CI 重新出 **`0.1.0+1475a08`**（含 47f69f8 全部修复 + 编译修复）。**正确可装版本 = `0.1.0+1475a08`，不是 47f69f8。**
