# 2026-08-04 工作日志

## 左缘"亮胶囊不返回、右缘能退"根因定位（ObackManager.m）

用户反馈：某些 App 的部分页面，左缘手势只出胶囊、页面不返回，右缘却能正常返回。

### 因果链（已对照代码确认）
- 左缘走 方案A：`driveSystemNavPopBeginWithPan:`（ObackManager.m:905→1473）把 Oback 自己的 pan 喂给系统私有 `handleNavigationTransition:`（:1542 `_callSystemNavPop:`），驱动**系统交互式 pop**。
- 该交互返回能否真正启动，受 App 对页面 `interactivePopGestureRecognizer` 的门控：横向 UIScrollView/WKWebView 冲突抑制、App 在 viewDidAppear 设 `enabled=NO`、自定义 delegate 返回 NO 等。被拦截时 `handleNavigationTransition:` 被调用但转场不启动 → 页面不动，但 Oback 边缘手势独立触发 → 胶囊照亮。
- 右缘走 `rightSimplePop`：`endTransition`（:1189-1212）松手直接 `[nav popViewControllerAnimated:]`，**绕过所有手势门控**，只要 `viewControllers.count>1` 必退。故同一页面右缘有效、左缘哑火。
- **更正（同日复审）**：之前误判该兜底为"死代码"。实际 `currentParallaxToView` 是 manager 路由标志，左缘标准 nav pop 时仍被置 YES（:781「标准 nav:系统原生交互转场(跟手)」、:946），**并非**视差移除后恒为 NO（那是 animator 的 `parallaxToView` 属性，已被 34d552f 删）。故 :1107-1158 探测+兜底对**方案A 左缘是生效的**（grep 确认 :781/:946 `currentParallaxToView=YES`，:1135 条件满足即跑）。
- 探测机制：首次横拖查 `transitionCoordinator.isInteractive`。标准页=YES→不兜底继续跟手；微信等自定义 nav=NO→切 `popViewControllerAnimated:` 兜底。
- **用户"某些页亮胶囊不返回"的真正原因**：这些页系统交互转场**确实启动了**（`isInteractive=YES`，被欺骗），但页面**实际没动**（App 在更深处拦截：自定义容器/子 VC 持有可见内容、或改造 `interactivePopGestureRecognizer` 使转场"建起不渲染"）。`isInteractive` 信号失效 → 兜底永不触发 → 空转亮胶囊。右缘 `popViewControllerAnimated:` 直连容器那层故照退。
- 结论：**不是"没加兜底"（兜底早就在方案A 跑），是兜底的探测信号 `isInteractive` 对这些页失效**。纠结点是"怎么可靠识别这些页"——`isInteractive` 不够；改用"观察前几帧 scrub 期间顶层 VC view 位移是否真变"更准，但有假阳性风险（过早判 gated 会把本该跟手的普通页降级成不跟手直接退=真·体验变差），故历史上保守未贸然上线，非"兜底体验差"。

### 建议修复（未实施，待用户确认）
换探测信号：方案A 左缘 `handleNavigationTransition:` 启动后，观察数帧内顶部 VC view 的实际位移/transform；手势推进但 view 不动 → 判定 gated → 立即 `popViewControllerAnimated:` 收尾（同右缘）。需真机校准观察帧数/位移阈值以防假阳性误伤普通页。

### 当前仓库状态
local main = origin/main = 30dbc0f（08-01 已从 GitHub 快进同步，15 commits）。本环境 GitHub 同步走 HTTPS（SSH 在此沙箱 DNS 失败）。

## 用户提案：左缘"排除列表"（按 App 勾选，左缘交还系统/不接管，保留右缘+弹窗）

用户想新增一个**独立于全局黑白名单**的"左缘排除列表"：勾选的 App 左缘不再由 Oback 接管（解决体验差/闪退），但右缘返回 + 弹窗 dismiss 仍生效。例：拼多多左缘闪退、QQ 左缘影响中间手势返回动画。

### 与现有机制的层级区分（关键）
- `isAllowed`（ObackPreferences.m:108）：**全局按 App 二值门控**——NO 则 Oback 完全不注入（左/右/弹窗全无）。是"这个 App 整不整用 Oback"。
- `_navPopShouldDriveSystemNav:`（ObackManager.m:1529）：**左缘按 nav 类决策**——微信/MMUI* 返回 NO 走 rightSimplePop 非交互；标准 nav 返回 YES 走方案A。目前只硬编码微信。
- `_isExcludedViewController:`（:672）：按 VC 类排除，当前恒返回 NO（空壳）。
- **新列表 = 第三层：左缘按 App 排除，但 Oback 仍注入（右/弹窗保留）。≠ 全局黑名单。**

### 实现要点（设计，未实施）
- 数据：新 pref key `leftEdgeExcludeApps`（NSArray），新列表控制器 `ObackLeftExcludeListController` 复用 `ObackAppListController`（mode="leftedge" → `_storeKey` 返回 @"leftEdgeExcludeApps"，:157-160 改一分支即可）；Root.plist 在「生效范围」组加 `PSLinkCell` 指向它。
- 行为闸门两处：
  1. `Tweak.xm` 四处 `self.interactivePopGestureRecognizer.enabled = NO`（:175/189/200/218）加守卫 `if (![ObackPreferences isLeftEdgeExcluded])`——排除的 App **不关系统原生左缘**，交还 App 自身返回（若有）。
  2. `shouldBegin`（ObackManager.m:730 附近）左缘 nav pan 若 `isLeftEdgeExcluded` 则早 return NO（不亮胶囊、不接管），右缘/弹窗不受影响。
- 微信硬编码（:1531/1534）保留独立——它是"左缘仍由 Oback 非交互接管"，与"左缘完全交还系统"是两种不同行为，勿并入新列表。
- 诊断：`[Oback-diag]` 横幅增加 leftEdgeExcluded 状态便于核对。

### 两个必须向用户澄清的风险点
1. **排除列表 ≠ 解除注入**：Oback 仍注入（为右/弹窗）。若某 App 的闪退发生在注入/setDelegate/hook 层面（非手势路径），排除左缘**救不了**，仍需全局黑名单。需用户确认拼多多闪退点。
2. **QQ 中间手势**：排除左缘后 Oback 不再劫持左缘且不关系统 interactivePop，大概率解除对 QQ 中间返回动画的干扰；但右缘 `rightSimplePop` 仍活跃、可能与 QQ 中间手势共享 nav——需真机验证是否彻底解决（894346d 已加 `_obLinkRightEdgeOpponentPansIfStale:` 治右缘抢中间）。
3. 排除 App 若自身未启用系统 interactivePop → 左缘变"无返回"（同未装 Oback 的 stock 行为），属预期。
4. **用户 11:09 确认语义：排除后「交还系统」**（App 有原生左缘返回则走原生，无则同 stock 无返回），非「左缘彻底失效」。设计锁定，待用户决定何时落地实现。

## ✅ 左缘排除列表 已实现并推送（commit 91e9176 → origin main，CI 出 0.1.0+91e9176）

用户选择"现在实现并推送（推荐）"。落地改动（CRLF 保留，内容级 diff +97/−3，无整文件噪声）：
- `ObackPreferences.h/.m`：`+isLeftEdgeExcluded`（读 `leftEdgeExcludeApps`，跨 App 全局 plist 真相源 + NSUserDefaults 兜底，复用 `_bid:matchesList:` 大小写/点前缀匹配）。
- `Preferences/ObackAppListController.h/.m`：薄子类 `ObackLeftExcludeListController`（mode="leftedge"→`_storeKey`=@"leftEdgeExcludeApps"，自动复用现有勾选/存储逻辑）。
- `Tweak.xm`：4 处 `interactivePopGestureRecognizer.enabled=NO`（setDelegate/viewDidLoad/viewDidAppear/viewDidLayoutSubviews）加 `if(![ObackPreferences isLeftEdgeExcluded])` 守卫——排除 App 不关系统原生左缘。
- `ObackManager.m`：`shouldBegin` 左缘 nav 命中排除列表早 `return NO`（不亮胶囊不接管）；`_linkNavPopGesturesInWindow` 跳过禁用保留系统 interactivePop；`[Oback-diag]` 横幅加 `leftEdgeExcluded=%d`。
- `Preferences/Resources/Root.plist`：生效范围组「选黑名单程序」后新增「选左缘排除程序」PSLinkCell → `ObackLeftExcludeListController` + 说明 footnote（左缘交还系统、右缘+弹窗保留、≠黑名单）。

**关键一致性确认**：`shouldBegin` 早 return 早于 :778 的 `interactivePopGestureRecognizer.enabled=NO`，故排除 App 不会被二次禁用；右缘/弹窗路径完全不受影响。微信硬编码 `_navPopShouldDriveSystemNav:` 分支独立保留（与"左缘完全交还系统"是两种行为，未并入）。

**待用户真机验证**：
① 拼多多左缘是否仍闪退（若闪退点在注入/setDelegate 层而非手势路径，排除左缘救不了，需转全局黑名单——风险点1未闭环）；
② QQ 排除左缘后中间手势返回动画是否恢复、右缘是否仍正常（风险点2）；
③ 设置面板「选左缘排除程序」勾选/保存/跨 App 生效；④ 非排除 App 左缘行为无回归（胶囊仍正常、不误交还）。

## 真机验收结果（用户 11:50 反馈）
- **拼多多商家版左缘闪退：排除后已好** → 证实闪退发生在左缘手势路径（非注入/setDelegate 层），左缘交还系统精确命中，风险点①闭环。
- **QQ 中间手势返回动画：排除后无效** → 证实 QQ 问题机制不在左缘，而在 `_obLinkRightEdgeOpponentPansInWindow:`（894346d）的右缘对手手势静态 `requireGestureRecognizerToFail:` 链接：它对**所有 App 统一执行**，`_linkNavPopGesturesInWindow` 的左缘排除早退（:549-552）只跳过「禁用 interactivePop」，**没跳过这个右缘链接**。故把 QQ 加进左缘排除列表，Oback 右缘 pan 仍压着 QQ 的 pan（含中间全屏手势）→ QQ 中间手势仲裁被拖慢、动画被压制。风险点②坐实。
- **结论**：左缘排除列表语义正确（对拼多多这类纯左缘问题有效），但它**从设计上修不了 QQ**（QQ 是右缘链接问题）。要修 QQ，必须让 Oback 对 QQ 也交还右缘（使 Oback 右缘 pan 让行于 QQ 的 pan），即把「排除」的覆盖范围从「仅左缘」扩展到「左缘+右缘链接」（弹窗 dismiss 仍保留 Oback）。待与用户确认是否扩展排除语义后实施。

## QQ 左缘排除无效的根因（14:05 续）
- 左缘排除只早退了 `_linkNavPopGesturesInWindow` 的「禁用 interactivePop」步（:550-552），
  但**右缘对手链接**有两处调用：初始 `:576` 与**懒补链** `_obLinkRightEdgeOpponentPansIfStale:`
  -> `_obLinkRightEdgeOpponentPansInWindow:`（:643）。
- 懒补链在 Oback 右缘 pan `gestureRecognizerShouldBegin` 时触发，对排除 App 未做守卫
  -> QQ 进会话后懒加载的右缘手势仍被 `[qqPan requireGestureRecognizerToFail: obackRightPan]`
  静态链接 -> QQ 中间手势被压制（正是用户报的「中间手势动画没好」）。
- 故左缘排除从设计上修不了 QQ：元凶是右缘静态链接，不是左缘。
- **修正方案（待用户确认）**：在 `_obLinkRightEdgeOpponentPansInWindow:` 顶部对 `isLeftEdgeExcluded`
  早退——既拦初始调用（冗余安全）也拦懒补链（真正元凶）。效果：排除 App 的 pan 不再被
  requireToFail 压住 -> QQ 中间手势释放；Oback 右缘返回仍由自然边缘优先级提供
  （无 requireToFail，edge pan 先 begin -> QQ pan 失败），**非「交还右缘」、非「黑名单」**。
  需真机验证右缘是否仍 Oback 独占、无双触发/QQ 抢右缘。

## 修正已落地（14:20，commit a6bca01 → origin/main，CI 出 0.1.0+a6ca01）
- 改动：`ObackManager.m` 的 `_obLinkRightEdgeOpponentPansInWindow:` 顶部加 `isLeftEdgeExcluded` 早退（拦初始+懒补链）；`Root.plist` 「选左缘排除程序」footnote 同步语义。
- CRLF 保留，diff 内容级（ObackManager.m +8 / Root.plist 仅 footnote 替换）。
- 真机验证清单：① QQ 进会话中间手势动画是否恢复；② QQ 右缘滑是否仍 Oback 独占（无双触发/QQ 抢右缘）；③ 拼多多商家版右缘是否仍正常返回；④ 其他未排除 App 无任何回归。

## 撤回 a6bca01 的右缘早退（14:35，QQ 双返回回归已修）
- 用户验 a6bca01：QQ 中间手势动画不仅没好，还造成右缘双返回。
- 根因：早退撤掉了 `_obLinkRightEdgeOpponentPansInWindow:` 的 requireToFail 硬链接 + `:851` 懒补链两道安全网
  -> QQ 右缘 pan 与 Oback 右缘 pan 失去失败依赖 -> 右缘起滑同时识别 -> 双返回。
- 中间手势没好：其冲突只在右缘（Oback 吞 QQ 全屏返回动画），中间起滑 Oback 不 begin；
  「松开右缘链接」打错靶，既没修中间手势又引入双返回。证明 QQ 不能用「左缘排除列表」框架修。
- 处置：撤回早退（ObackManager.m :602 顶部恢复原状），Root.plist footnote 改回纯左缘语义（已 commit+push）。
  左缘排除列表仅治左缘（拼多多类闪退）；右缘/QQ 问题需另立独立、经真机验证且不引入双返回的方案。

## QQ 问题根因回溯（14:45，定位引入源）
- `git merge-base --is-ancestor 894346d e140dca` = 是祖先 → 894346d 在当前 main 链上（本地对象仍在，未被丢弃）。
- 894346d (2026-07-26) "根治微信双返回/右缘空白/QQ右缘冲突/黑名单失效" 是引入 QQ 手势接管机制的转折点：
  引入 `rightSimplePop` + `gestureRecognizer:shouldRequireFailureOfGestureRecognizer:` 单向让步。
- 后续加固：60ab548 引入 `_obLinkRightEdgeOpponentPansInWindow:`（requireToFail 链接压 QQ 自带 pan）、
  678169d 右缘不让步(:1025)。三者叠加 = Oback 吞掉 QQ 右缘/全屏返回手势 = 用户报的「中间手势没好」。
- 用户「以前正常」= 894346d 之前（5674b9a 及更早，QQ 用自家手势，微信双返回未完全根治但 QQ 正常）。
- 性质：这是权衡非纯回归——894346d 主要修的「微信双返回」仍重要，盲目回退该系列会复发微信双返回。
  需独立新方案：保留微信双返回修复 + 黑名单修复，同时让 QQ 中间手势恢复、且不双返回。
- git -S 定位：rightSimplePop 首现 894346d；_obLinkRightEdgeOpponentPansInWindow 首现 60ab548；
  右缘不让步(:1025)首现 678169d。

## GitHub 优化分析（用户要求，纯分析未改代码，14:50）
- 结论：Oback 性能层已优化到位（_mergedPrefs 2s TTL 缓存 / OBLog 门控 / CADisplayLink 仅手势中 / shouldBegin 轻量补链），勿在性能层重复造。
- 最大优化机会在功能层：① 全屏/任意位置返回（对齐 iOS 26 + PanCake，lwlsw/PanCake 启发），顺带终结 QQ 中间手势之争；② 触发区放宽（triggerWidth 已存在可放开）；③ 每 App 手势档案（收口黑名单/白名单/左缘排除三套）。
- 工程层：设置→tweak 实时生效（Darwin 通知失效缓存，替代 2s TTL）；诊断一键 dump。
- 兼容性 P2：QQ/微信手势仲裁需 shouldRecognizeSimultaneouslyWith + 按 view 类精确裁决，单列专项、先诊断后动。
- 参考项目：PanCake(lwlsw)、HomeGesture1315(Tweaker177)、SquidGesture、SwipeBack(Ryan Petrich)、SCSafariViewController。

## 全局返回开关 + 右缘dismiss 最终锁定（15:03 续）
- 设计确认：新增「全局返回」开关，默认关，仅勾选 App（globalBackApps 列表 + ObackGlobalBackListController 薄子类，照 ObackLeftExcludeListController 模式）开启。
- 开启的 App 自动排除 Oback 左右缘（单一手势源，杜绝边缘+全屏竞争双返回，正是 a6bca01 翻车根因的规避）：shouldBegin 对 Oback 左右缘 return NO、系统 interactivePop 禁用防双触发；仅挂全屏 pan 接管左→右 nav pop（左侧热区 + 横向速度占优判别）。
- 右缘 modal dismiss：用户 15:03 选定「保留 Oback 右缘 dismiss」（方案B 不变）。理由：右缘一直好用、不闪退，modal 需关闭兜底。
- 边缘/视差/微信仲裁原样保留，仅这些 App 不走边缘路径；其他 App 100% 不变。
- 落地步骤预告：ObackPreferences 加 isGlobalBackEnabled（读 globalBackApps）；Tweak.xm shouldBegin 加左右缘早退；ObackManager 加全屏 pan 挂载 + 判别；Root.plist 加「选全局返回程序」PSLinkCell。

## ✅ 全局返回 已实现（待 commit/push → CI）
- 改动文件（CRLF 保留）：
  - `ObackPreferences.h/.m`：`+isGlobalBackEnabled`（读 `globalBackApps`，复用 `_bid:matchesList:` 匹配）。
  - `Preferences/ObackAppListController.h/.m`：薄子类 `ObackGlobalBackListController`（mode="globalback"→`_storeKey`=@"globalBackApps"）；Root.plist 在生效范围组加「选全局返回程序」PSLinkCell + 说明。
  - `ObackManager.m`：
    - `attachToWindow`：globalBackEnabled 时不挂 window 左缘 panL（右缘 panR 照常 + 挂全屏 pan）；`pans` 数组按 panL 是否挂载条件构造。
    - `_attachNavPanToNav`：globalBackEnabled 时跳过 nav.view 左缘 pan（右缘照常），仅单一手势源接管左→右返回。
    - `gestureRecognizerShouldBegin`：globalBackEnabled 且 `edge==Left`（不论 kind=window/nav）一律 `return NO`——左缘交全屏 pan 接管；右缘放行（modal dismiss）。
    - 全屏 pan `_globalPanShouldBegin:`（左~1/3 热区 + 有 nav pop 才允许识别）/`handleGlobalPan:`（Began 只记起点不驱动；Changed 横向占优才 beginTransition 驱动 nav pop，否则取消交还 App）/`_cancelGlobalPan:` 复用已验证转场链路。
- **关键设计修正（Task #36 收口）**：原设想在 Tweak.xm 4 处 `interactivePopGestureRecognizer.enabled=NO` 加 `!isGlobalBackEnabled` 守卫 —— 此改会让全局返回 App 重新启用系统原生 interactivePop，与全屏 pan 同时驱动 `_UINavigationInteractiveTransition` → **双返回**。故该 Tweak.xm 改法**刻意不做**；native interactivePop 对 globalBack App 仍保持禁用（与「系统 interactivePop 禁用防双触发」一致）。左缘排除改在 ObackManager.m 内（不挂左缘 pan + shouldBegin 广守卫）实现，Tweak.xm 无需改动。
- 右缘 modal dismiss（panR）+ nav.view 右缘（rightSimplePop）对 globalBack App 一律保留。
- 待办：commit + push origin main 触发 CI（0.1.0+<sha>）。真机验证：①勾选 App 左~1/3 横向滑可返回、不误吞滚动；②右缘 dismiss 仍正常；③其余 App 零回归；④全屏 pan 与右缘无竞争。

## ✅ 全局返回 多 window 崩溃修复（e6a02ac → origin/main，CI 出 0.1.0+e6a02ac）
- 根因：`_globalPan` 是单例单 ivar；同一 globalBack App 的 `attachToWindow:` 被第二个 window（如键盘 UITextEffectsWindow / 多 scene / 自建第二 UIWindow）再调用时，`_globalPan = panG` 覆盖 → 第一个 window 的全屏 pan 变孤儿。孤儿 pan 在 `gestureRecognizerShouldBegin:` 身份比较 `(id)pan == (id)_globalPan` 失败 → 漏进边缘分支，第747行 `(pan.edges & ...)` 对普通 UIPanGestureRecognizer 发 unrecognized selector → **崩**。
- 修复：删 `_globalPan` ivar；给全屏 pan 打关联对象标记 `kGlobalPanKey`（第51行原已定义未用），`shouldBegin` 改 `if (objc_getAssociatedObject(pan, kGlobalPanKey))` 分流；并去掉全屏 pan 上的 `kPanKindKey=@"nav"`（否则孤儿路径仍当 nav 边缘 pan 而崩）。每个 window 的全屏 pan 自带标记，多 window 也正确分流、永不进边缘分支。行为零变化（单 window 识别等价）。

## 全局返回「跟手 / 非交互」行为澄清（回答用户）
- **标准 nav（绝大多数 App）：跟手。** 全局返回复用 方案A 系统原生交互转场：横向意图确认后 `beginTransition:` → `driveSystemNavPopBeginWithPan:` 把我们的全屏 pan 喂给系统私有 `handleNavigationTransition:`，`updateTransition:` 持续把**同一 pan** 喂进去做 scrub（:1316 `_callSystemNavPop:`），页面随手指走；`endTransition:` 喂最终 pan 由系统按速度/位移决定 commit/cancel —— 与原生 iOS 返回一致。
- **与左缘边缘返回的区别**：边缘返回在 Began(零位移)就启动转场；全局返回为不误吞 App 内滚动，加了约 6pt 横向占优门控（dx>6 且 v.x²>v.y²×1.69）才启动 —— 开头有个极小死区，确认后完全跟手。
- **微信等自定义 nav（rightSimplePop）：不跟手，松手才瞬返。** `_navPopShouldDriveSystemNav:` 返回 NO → `rightSimplePop=YES` → `beginTransition` 不启动交互转场，`endTransition` 松手 `popViewControllerAnimated:` 非交互返回。但这与该类 App 的**左缘边缘返回行为一致**（系统原生返回本身就不跟手，Oback 对它们只能非交互返回），非回归。
- **结论**：不会「一滑就直接返回」。只有微信类才松手瞬返；标准 App 完整跟手。唯一前提是 globalBack App 走方案A（默认），非微信类。

## 酷安实测 = 瞬返（推翻「标准 nav 跟手」预测，根因已定位）
- 用户在酷安开全局返回实测：**瞬返（不跟手）**。证明酷安走 `_navPopShouldDriveSystemNav:` 的 NO 分支 → `rightSimplePop` 松手瞬返；但原因**不是微信**（`bid=com.coolapk.market` 非 `com.tencent.xin`、`navCls` 非 `MMUI*`）。
- 根因：`navPopSystemTargetForNav:`（:1605）读 `nav.interactivePopGestureRecognizer._targets[0].target` 返回 **nil** → `return (t!=nil)` → NO。即酷安 nav 取不到系统交互转场 target。
- 推断：酷安自研了全屏/边缘返回手势（国内 App 常见），把系统 `interactivePopGestureRecognizer` 的 `_UINavigationInteractiveTransition` target 剥离或整体换掉 → Oback 无法把 pan 喂进系统 `handleNavigationTransition:` 跟手。
- **与全局返回功能无关（固有状态）**：酷安不开全局返回、仅开普通左缘返回，照样瞬返（标准左缘也走同一个 `driveSystemNavPopBeginWithPan:`→`_navPopShouldDriveSystemNav:`）；只是用户此前从没在酷安开过 nav pop 类功能，现在首次撞见。
- 处置（用户选「先打诊断日志坐实」）：在 `navPopSystemTargetForNav:` 加细分日志 `[diag-navTarget]`，仅对「非微信类且 target=nil」打印 `bid / nav 类 / ipg 是否存在 / enabled / targets.count / delegate`，定位酷安是「ipg 仅 disabled」(可重 enabled 后喂系统转场) 还是「target 被剥」(必须走 Oback 自研交互转场=nav 视差自定义 animator 链路，已做阴影渐隐无 reparent)。已 commit+push 触发 CI。
- 后续分支（待真机日志坐实后定）：① 仅 disabled → 对该 nav 重设 interactivePopGestureRecognizer.enabled=YES 并喂我们的 pan；② target 被剥 → 对该 nav 改用 Oback 自研交互转场（扩展 navParallaxEnabled 链路，风险=需真机验底部空白/导航栏/scrollView 三件套）。③ 或接受瞬返不动。

## CI 编译失败 + 修复（1a8191a → origin/main，CI 出 0.1.0+1a8191a）
- 412167b 在 GitHub Actions 编译挂：`ObackManager.m:1622 property 'enabled' not found on object of type 'id'`（`-Werror` 直接拒）。
- 根因：`navPopSystemTargetForNav:` 里 `ipg = nav.interactivePopGestureRecognizer` 经 `valueForKey:` 取回是 `id` 类型；诊断 OBLog 里直接写 `ipg.enabled` / `[ipg delegate]` → 编译器不认 `enabled` 属性（它是 `UIGestureRecognizer` 的）。
- 修复：`(int)((UIGestureRecognizer *)ipg).enabled` 与 `[(UIGestureRecognizer *)ipg delegate]` 两处 cast。`-Werror` 下对 `id` 直接点属性/消息务必先 cast 到具体类，这是本项目铁律（之前 autorelease/__weak 也是 -Werror 拒的同源约束）。
- 提醒：该日志需「调试日志」开关打开才会写盘（OBLog 首行硬门控 `[ObackPreferences debugLogEnabled]`）；用户若想拿酷安 `[diag-navTarget]` 详情须先开开关。

## 用户回传日志 oback_debug(1).log 分析（17:58 那次）
- 用户发来的 `D:/Backup/Documents/xwechat_files/.../oback_debug(1).log` **不是酷安日志**：全篇只有 `TGTextEditorController`(Telegram)、`com.tencent.xin.sharetimeline`(微信)、`UINavigationController`、`MSELaunchNavigationController`(微信)，搜不到 `com.coolapk.market` / `coolapk`。
- **整份没有 `[diag-navTarget]` 行** → 装的是诊断修复前的旧 .deb（全局返回功能在、但 nav-target 诊断未生效；诊断在 412167b 加但编译挂、1a8191a 才修好，最新可装=0.1.0+d3c20a6）。
- `[diag] pan touchesBegan` 是旧有的触摸起点日志（非本诊断）；`attachNavPan: pans=2 (左缘+右缘)` 证明左缘仍挂、即该 App 未在 globalBackApps 命中或 build 旧。
- 结论：仍缺酷安实测数据。须重装 0.1.0+d3c20a6 → 确认酷安在 globalBackApps → 删旧日志 → 酷安内左 1/3 真横滑触发返回 → 取回含 `bid=com.coolapk.market` 的 `[diag-navTarget]` 行。
