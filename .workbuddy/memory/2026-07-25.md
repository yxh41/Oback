# 2026-07-25

## 微信「一次滑动双返回」根因再辨析 + 结构性加固（commit ef730c9）
- **用户关键纠正（重要调试方法论）**：此前建议「关掉微信分组插件验证双返回」是**无效测试**——分组插件 ON 才存在「主界面→分组列表→聊天」三层结构；关掉插件三层塌成两层，双返回的物理前提消失，故「关插件后不双返回」既不能证明是插件手势、也不能证明是系统原生手势，只是因为没了第三层。✅ 此纠正成立，已采纳。
- **正确隔离测试**（用户称测试不了）：插件保持 ON（三层在），只切 oback 对微信的开关——oback 关(微信拉黑)仍双返回→与 oback 无关(原生或插件)；oback 开→单返回→修复覆盖到了。
- **结构性加固（不依赖「对手是谁」）**：`ObackManager.m` `_linkNavPopGesturesInWindow:` 新增第二道防线——递归枚举窗口视图树里**所有 `UIScreenEdgePanGestureRecognizer`**（含越狱插件私有的边缘返回手势），统一 `requireGestureRecognizerToFail:我们的 pan`。第一道仍是关掉 nav 原生 `interactivePop`。
  - 安全性：只碰 `UIScreenEdgePanGestureRecognizer`，**不动滚动用的普通 `UIPanGestureRecognizer`**；我们的 pan 是 `UIPanGestureRecognizer` 子类，被 isKindOfClass 过滤天然排除；视图树递归加 `depth>40` 护栏防爆栈。
  - 效果：无论对手是系统原生还是插件私有边缘手势，同一根手指只认 Oback 单次 pop，双返回从结构上不可能发生（含插件场景）。
- 推送：`76798c8..ef730c9 main -> main`，CI 应出 `0.1.0+ef730c9`。
- **待验证**：装 `0.1.0+ef730c9`，微信分组插件保持开，聊天界面左边缘滑一次应只回一层（聊天→分组列表），需再滑一次才到主界面。若仍双返回，则可能是插件用**非边缘手势**做返回(普通 pan/自定义)，届时需另寻方案(如枚举所有 pan 但需排除滚动——风险更高，先不碰)。

## 用户实测加固后仍双返回 → 真因修正（重要：推翻上节「插件非边缘手势」猜测）
- **实测**：`ef730c9` 后仍双返回。用户问「还有没有别的原因」→ 重新读 `_linkNavPopGesturesInWindow:` 调用链，发现**真因在我的代码时机，而非插件手势**：
  - 链接只在 `windowBecameKey:` / `start`(+延迟1s) 时做**一次**（ObackManager.m:125/129/142/148/158）。
  - 微信「主界面→分组列表→聊天」是**同一 window 内 push**，window **不会再次变 key** → 分组容器 nav/边缘手势在初次链接之后才压入视图树 → 枚举时不在树里 → **从未被链接** → 加固对那个容器形同虚设。
  - 故双返回还在，最可能是「链接时机漏了后创建容器」，比我之前甩的「插件非边缘手势」更站得住、更该修。
- **根治方案（未做，用户说先不管）**：把链接从「window 变 key 一次性」改为「手势真正接管时实时补链」——在 `gestureRecognizerShouldBegin:` 判定 `shouldBegin=YES` 那一刻，对当前 topMost 所在 nav 再调一次 `_linkNavPopGesturesInWindow:`（幂等无害）。容器何时创建都能在滑动前被压住。一行调用、低风险。
- **方案本身绕不过的盲区（真·罕见情况）**：加固只枚举 `UIScreenEdgePanGestureRecognizer`、不碰普通 `UIPanGestureRecognizer`（怕误伤滚动）。若分组插件用普通 pan 做返回，则 `requireGestureRecognizerToFail:` 压不到——需枚举普通 pan 并加滚动白名单过滤，代价大、易误伤，目前不碰。
- **其他改法副作用审视（已告知用户）**：① `requireGestureRecognizerToFail` 成对依赖，极端下若 pan 卡未失败态会使原生/插件边缘手势永久不 begin（极低：pan 生命周期正常）；② `windowBecameKey` 每次遍历视图树，频繁切 window 的 App 有轻微开销（depth>40 护栏+轻量，可接受）；③ 调试日志关闭时 OBLog 首行 return 零开销；④ 快滑零位移兜底(3c985fa)极快蹭边缘会误返（设计权衡，用户认可）；⑤ 灵敏度滑块/动量投影阈值调太低会误触发（默认 400/0.30 合理）。

## 实时补链修复已落地（commit a62eded，根治双返回）
- **做法**：在 `gestureRecognizerShouldBegin:` 的 `shouldBegin=YES` 分支（haptic 之后、return YES 前）加一行 `[self _linkNavPopGesturesInWindow:win];`。
- **为什么来得及压住当次滑动**：`requireGestureRecognizerToFail:` 是成对依赖，系统**每个 touch 事件都重评估**；我们的 pan 在手指移动期间保持交互态(Changed)不会失败，故对方边缘手势整段被阻塞，只弹一层。依赖设一次即持久，后续滑动也生效。
- **性能/功耗/App 影响（用户明确询问，评估结论均为「可忽略/无损」）**：
  - 触发频率：仅 edge+可返回 时 `shouldBegin=YES` 才调一次，**一次滑动一次**，非每帧。
  - CPU：遍历窗口视图树(depth>40 护栏)，纯内存属性设置 + 加依赖，**无 IO/布局/绘制**，亚毫秒~几毫秒级。
  - 功耗：无后台任务/定时器/轮询，对电池无 measurable 影响。
  - 对 App：仅当**我们接管**时压制对方边缘手势；`shouldBegin=NO`(非边缘/不可返回/横向滚动让位)时对方 `requireGestureRecognizerToFail` 依赖不阻塞(我们的 pan 没 begin→对方正常 begin)，故 App 自有边缘手势功能**完全保留**；滚动用的普通 pan **不枚举**，横滑/滚动不受影响；成对依赖极端卡死风险极低(pan 生命周期正常)。
- 与既有 `windowBecameKey` 一次性链接互补不冲突（前者覆盖初次，后者兜底后创建容器）。
- 推送：`26d630d..a62eded main -> main`，CI 应出 `0.1.0+a62eded`。**待真机验证**：微信分组插件保持开，聊天界面左边缘滑一次应只回一层。

## 新增「双返回诊断」开关（commit 08fb8e9）
- **用户认可**：上一轮问「要不要顺便加诊断开关」，用户回「好的」→ 落地。
- **做法**：`Root.plist` 调试分组新增 `PSSwitchCell`「双返回诊断」(key=`doubleReturnDiag`，默认关)；`ObackPreferences.doubleReturnDiagEnabled` 读取；`ObackManager._linkNavPopGesturesInWindow:` 末尾调 `_diagLogEdgeGesturesInWindow:` —— 该函数在开关开启时枚举窗口视图树所有 `UIScreenEdgePanGestureRecognizer`，打印**精确类名 + 宿主视图类**（`diag[双返回]: window=.. | 边缘返回手势共 N → [...]`）。
- **判读**：原生系统手势恒为 `UIScreenEdgePanGestureRecognizer`；清单里**任何其它类名**即 App/插件私有边缘返回手势 → 若双返回仍在，对照多出来的类名即可定位「第二层」是谁。
- **节流 + 门控**：同一 window 每 2s 最多打一次（避免每次起滑刷屏，用 `kDiagLastLogKey` associated 时间戳）；输出走 `OBLog`，故**需同时开启「调试日志」**才可见；关闭零开销。
- 推送：`887282e..08fb8e9 main -> main`，CI 应出 `0.1.0+08fb8e9`。

## 分析 oback_debug(14).log（微信卡顿排查，commit 未改代码）
- **结论**：微信「有时卡」**不是 oback 主因**，根因是微信装太多越狱插件。oback 自身开销与流畅的华为健康同量级。
- **铁证（日志硬数据）**：
  1. `linkNav` 耗时：微信 0.3–0.9ms(链接6-7个)，华为健康 0.07–0.46ms(**链接8个**)——华为挂着更多边缘手势、跑同一套 oback 却流畅 → 若 oback 是元凶，华为必卡。它没有。
  2. 微信窗口运行期从「链接3个」累积到「链接7个」边缘手势(插件注入)，但华为8个仍流畅 → 手势数量非区分项，App/插件环境才是。
  3. 卡顿时段日志里 oback 零活动：oback 纯事件驱动，无后台线程/定时器/逐帧轮询，只有边缘起滑才干活。
- **oback 唯一热路径**：全屏 pan 挂 window，每个触摸过 `gestureRecognizerShouldBegin:` → 非边缘=2次 NSUserDefaults读+边缘判定(微秒级return NO)；仅边缘才走 view 树遍历。华为同样逻辑流畅 → 边际贡献极小，只是给微信本就拥挤的 per-touch 手势仲裁链多添一个识别器。
- **真正元凶**：微信+多插件 hook 进 UIKit touch/scroll/appearance，每个触摸事件触发插件代码；oback 全屏 pan 在此之上叠加 = 插件负担×全局手势。主因插件，oback 至多边际。
- **干净隔离测试（与之前无效「关插件」测试不同：保留三层结构）**：把 `com.tencent.xin` 加 oback 黑名单 → oback 对微信完全不接管、插件仍开。卡顿还在=100%插件；卡顿没了=oback 有边际贡献(再优化 per-touch 路径)。
- **可选 oback 微优化（未做，待用户拍板）**：内存缓存 `ObackParams`/`isAllowed`（设置变更通知刷新），去掉 `shouldBegin` 每触摸读 NSUserDefaults；零风险但据日志作用很小，治本是减微信插件。
- **双返回已确认修好**：该日志内微信左/右边缘均单次干净返回，无双返回（实时补链 a62eded 生效）。
- 推送：仅分析，未改代码；记忆 `de1fdb1` 已在 `08fb8e9` 之后。

## 用户决定：微信卡顿 / oback 微优化 全部暂缓（01:11 收尾）
- 用户原话「先不管了，就这样先。我后续需要再来解决」。
- **明确暂缓项**：① 微信黑名单隔离测试；② 可选 `ObackParams`/`isAllowed` 内存缓存微优化。两项均不做、不留 commit。
- **当前可发布状态（main HEAD 应 ≈ `08fb8e9` / `0.1.0+08fb8e9`）**：双返回根治、灵敏度滑块+极值显示、调试/双返回诊断开关、`OBLog` 门控、实时补链全部已落地且自测无闪退。可作为阶段性稳定版。
- **后续恢复入口**：回来先跑黑名单隔离测试确认微信卡顿是否插件主因，再决定是否做 per-touch 缓存优化。

## 新增「重置设置」按钮（01:16 起，commit 待 push）
- **用户需求**：设置页加「重置设置」功能。
- **做法（标准 PreferenceLoader 路径，不碰禁区）**：
  - `Root.plist` 末尾新增「维护」分组 + `PSButtonCell`（`label=重置设置`, `action=resetSettings`）。**未用 `cellClassForSpecifier:` 换自定义 cell、未用 `PSSpecifier setAction:`**——PSButtonCell 官方 action 打到 `ObackSettingsController`（NSPrincipalClass），绕开两处 SIGABRT/编译失败禁区。
  - `ObackSettingsController.m`（Preferences 子工程，已确认 `-fobjc-arc`）新增：
    - `resetSettings`：弹 `UIAlertController` 二次确认（取消/重置-破坏性）；
    - `_obPerformReset`：`[[NSUserDefaults alloc] initWithSuiteName:@"com.zlhkf.oback"] removePersistentDomainForName:` + `synchronize` → 清空整个域（含白/黑名单数组、各项滑块/开关）→ `_valueLabels=[NSMutableDictionary dictionary]` 丢缓存 → `[self reloadSpecifiers]` 重建 specifiers 并刷新表格，所有开关/滑块回弹 Root.plist 的 `<default>`。
  - 因 tweak 每次手势实时读 `NSUserDefaults`（`ObackPreferences` 各方法均 `initWithSuiteName:` 新实例），重置后运行中的 App **下次触摸即生效**，无需额外通知/杀进程。
- **验证**：`python plistlib` 校验 Root.plist 合法（27 项，末两项=维护组+重置按钮）；`.m` 尾部结构正确。
- **待真机**：装新包后在设置页点「重置设置」→ 确认二次弹窗 → 所有开关/滑块/已选白黑名单回到默认。
- **已 push**：`b90f222..769c6ec main -> main`（用户提供 GitHub PAT 临时凭据推送，用完即删，未落盘/未入库）。CI 应出 `0.1.0+769c6ec`。

## 用户疑问：设置 App 内「没有效果」是不是手势冲突？（诊断，非 bug）
- **用户猜测**：Oback 边缘返回在「设置」App 里无效，怀疑与设置自身系统手势冲突。
- **真相（代码确认，非冲突）**：`Tweak.xm` `%ctor` 开头 `if ([bid hasPrefix:@"com.apple."]) return;` —— 所有 `com.apple.*` 系统进程一律不注入。设置=`com.apple.Preferences` 被前缀命中，`%init(Oback)` 根本没跑，手势管理器未启动。即 Oback 在设置内完全未加载，不存在"冲突"前提（冲突需双方都在场）。
- **设计意图**：注入系统进程有 UI 异常/黑屏风险（同因也排除包管理器 Sileo/Zebra/Cydia 等）。属安全边界，有意为之。
- **用户在设置里看到的边缘返回是系统原生**（iOS 设置本就支持），能用，只是无 Oback 视差/动画。
- **可选后续（用户未拍板）**：若要 Oback 在设置生效，给 `com.apple.Preferences` 开白名单例外放出排除；风险=设置部分子页用自定义 present 转场，可能触发此前专门规避的黑屏，需真机小范围验。默认维持安全边界。

## 黑屏风险的「其他解决方式」分析（用户要求：只分析不动手，01:30）
- **澄清黑屏根因已变**：早期黑屏=全局 hook `presentViewController:` 强换 `transitioningDelegate`（已删，1dc096d）。**当前**仅手势驱动 dismiss 时临时套 `ObackTransitioningDelegate` + 方案 B（只动 sheet/fromView，不碰 presenting/toView、不加遮罩），第三方实测无黑屏。故 blanket `com.apple.*` 排除是**保守一刀切**，非"每个系统 App 必黑屏"。
- **残险唯一落点=modal-dismiss 路径**（且仅当某弹窗的 toView 上下文不满足方案 B「presenting 已在底下可见」假设）；**nav-pop 路径零风险**（只动导航栈上一页，与第三方同源安全）。Settings 近乎纯 nav 栈，价值高、风险低。
- **可行替代方案（按推荐度）**：
  1. **功能拆分**：系统 App 只开 nav-pop、关 modal-dismiss（`beginTransition` 检测到 modal 时退化为原生 dismiss）→ 设置拿 Oback 边缘返回、黑屏≈0。
  2. **精确放行**：`%ctor` 把 `com.apple.Preferences` 单列放行，其余 `com.apple.*`（尤其 `com.apple.springboard`）照排；可叠「系统 App 也启用」开关(默认关)。
  3. **presentation-style 熔断**：modal 分支查 `modalPresentationStyle`/原 delegate 类，遇 fullScreen/自定义私有/popover 退原生 dismiss（对所有 App 加固）。
  4. **弹窗路径不提供自定义动画**：系统 App dismiss 只给 `interactionControllerForDismissal` 搓原生 dismiss，ObackAnimator 不进弹窗 → 结构上不可能黑屏（牺牲弹窗视差）。
  5. **快照兜底 toView**：dismiss 时对底层 `snapshotViewAfterScreenUpdates:` 当 toView，堵「toView 黑/空」理论缺口（改动大、可能微闪）。
- **推荐落地组合（待用户拍板）**：② 精确放行 Settings + ① 系统 App 关 modal-dismiss + ③ style 熔断。默认行为不变，SpringBoard/包管理器永在排除名单。
- **铁律不变**：`com.apple.springboard`(SpringBoard) 必须永远排除——它是系统 UI 宿主，注入最危险。

## 启用系统 App（如设置）的性能/功耗影响分析（用户追问，01:37，只分析）
- **结论：可忽略，与现有第三方 App 同量级**。
- **论据**：
  1. 纯事件驱动：无后台线程/定时器/轮询/定位/网络 → 空闲零功耗；离开 App 即归零。
  2. 仅触摸时跑：非边缘触摸=2 次 NSUserDefaults 读+边缘判定(微秒级 return NO)；仅边缘+可返回才做视图树遍历(亚毫秒~几毫秒)。与第三方完全一致。
  3. **SpringBoard 仍排除** → 全局手势路由中枢不被加权，Oback pan 只挂设置自身 window（进程内），无系统级开销。
  4. 动画仅 compositor translate/scale，60fps 零压力。
  5. 方案「系统 App 关 modal-dismiss」反而省掉弹窗路径，比完整 Oback 更轻。
  6. 日志实测背书(oback_debug(14))：linkNav 微信 0.3–0.9ms / 华为健康 0.07–0.46ms 流畅 → 设置(比微信轻)不会卡。
- **唯一实打实 IO/功耗点**：debugLog 开着时每次手势写盘 `/var/mobile/oback_debug.log`（已有开关，日用机建议关）。启用设置后仅"设置手势也写盘"，关掉即零写盘。
- **无系统级累积效应**：每 App 独立进程，启用设置=只给设置进程加开销，不扩散到系统。

## 动画改进点分析（用户追问，只分析不动手，01:39）
- **现状**：`OBApplyParallax`(ObackTransition.m) 仅做 fromView 平移(+modal 缩小至0.92) + toView 缩放探出(0.92→1.0)+扁平黑遮罩(dimView alpha) + fromView 阴影；释放/取消固定 `params.duration` + easeOut，**忽略手指释放速度**。
- **高收益改进（按优先级）**：
  1. **释放动量继承 + 动态时长 + 弹簧收尾**（最大 premium 提升）：`UIViewPropertyAnimator`+`UISpringTimingParameters(initialVelocity:)` 带入手指速度，时长≈剩余距离/速度；告别固定 easeOut 脱节感。
  2. **取消弹簧回弹**（轻微 overshoot 再稳定），同思路。
  3. **上一层边缘阴影渐变 + 更强层叠分离**（当前仅 fromView 有阴影）。
  4. **遮罩改 CAGradientLayer 方向性渐变** 替代扁平黑，纵深更真（只动 alpha 不触发布局）。
  5. **移动页瞬时圆角 8–12pt 完成归 0**（卡片感；nav-pop 谨慎、建议可关开关；弹窗天然适合）。
- **保真/细节**：⑥ 确认拖动 percent 1:1 映射手指位移（非固定距离归一）；⑦ 阈值"抓取"微触感(UIImpactFeedbackStyleLight)；⑧ 尊重 `Reduce Motion` 降级淡入/瞬切；⑨ 边缘胶囊指示器阈值到达脉冲(实现待全读)。
- **约束**：nav-pop 路径安全可大改；**modal 方案B 须保守**(不碰 toView、不加易闪重合成)防黑屏；全程只动 transform/alpha/cornerRadius(GPU 合成)保 60fps，禁逐帧改 frame/bounds 触发重排。

## 动画改进已动手落地（commit 5a5039e，已 push 769c6ec..5a5039e → CI 应出 0.1.0+5a5039e）
- **用户拍板「好的，动手」**，按上面清单逐条实现（未全做，取舍见下）。
- **落地项**：
  1. **动量继承弹簧收尾**：`ObackManager.endTransition:` 记录 `releaseVelocity`(前向 pt/s) 与 `releasePercent`(0~1) 到 manager 属性（仅作读取，不动 finish/cancel 调用——冻结铁律不受影响）；`ObackAnimator.animateTransition:` 读取后 `usingSpringWithDamping:0.82` + `initialSpringVelocity:(vel/w)` 收尾；时长按剩余进度动态 `duration*(0.55+0.45*remaining)` 钳 0.22~0.46s → 拉得越多收得越快、"被吸过去"感。
  2. **方向性渐变遮罩**：纯黑 `UIView` 换 `CAGradientLayer`（沿边缘方向由深到浅，黑→透明 @0.55）；整体浓度仍由 `OBApplyParallax` 设 `dim.alpha=(1-percent)*dimAlpha` 控制，仅改质感不触发布局。
  3. **弹窗卡片圆角**：`cardCornerEnabled`(默认关) + `cardCornerValue`(默认12) → modal 下拉时 `fromView.layer.cornerRadius = cardCornerValue*percent`（CA 线性插值渐进圆角）；开启时 `applyShadowTo:` 自动放弃阴影避免与圆角打架。
  4. **Reduce Motion 降级**：`UIAccessibilityIsReduceMotionEnabled` → 回退线性 `easeOut`（与旧版一致）。
  5. **用户开关**：`Root.plist` 新增「动画」分组 + `PSSwitchCell`「弹性收尾」(key=`springEnabled`, 默认开)；`ObackPreferences.params` 读取三新键。
- **未做/取舍**：取消回弹 overshoot(②) 因 `OBApplyParallax` 把 percent 钳 [0,1]，反向 overshoot 不可见，保留平滑减速即可；边缘胶囊脉冲/瞬时圆角归零(⑤⑨) 暂未加，留待后续；`UIViewPropertyAnimator` 方案未采用，因 `UIPercentDrivenInteractiveTransition` 已驱动 `animateTransition`，直接换 `usingSpringWithDamping` 最低侵入、风险小。
- **零回归保障**：弹窗仍严守方案B（只动 fromView、不碰 presenting/toView、不加深遮罩）；仅动 transform/alpha/cornerRadius；finish/cancel 调用一处未改 → 冻结 bug 不会复发。
- **待真机验证**：① 各 App 左/右边缘内滑松手有"被吸过去"弹性收尾、快速甩动更快归位；② 遮罩呈方向性渐变(非纯黑块)；③ 设置「弹性收尾」开关可实时关(回退线性)；④ 弹窗下拉若开 `cardCornerEnabled` 有渐进圆角；⑤ 仍无界面冻结/黑屏。

## 分析 oback_debug(15).log（用户发来问"有没问题"）
- **头号发现：这日志是旧版包的** —— 全程无 `5a5039e` 新增的 `animator done (spring=... cancelled=...)` 行（旧行 shouldBegin/linkNav/indicator/beginTransition/endTransition 都还在，因为我没删）。即设备仍在跑升级前 .deb，CI 可能还没出或没装。**∴ 本日志无法验证新弹簧/渐变动画**，需装 `0.1.0+de9d0cd` 重抓 (16)。
- **日志本身健康**：18 次 endTransition 提交判定全部符合阈值——含动量投影正确提交 0.21/0.24 小拖动(vel 452/623>400 或 projected>0.30)；无 abort/failure 行(无冻结)；每次 childCount 单步递减(实时补链无双返回)；无崩溃。
- **18:10:02–18:10:10 连续 3 次 cancel**（percent 0.04/0.14/0.22, vel 0/8/119）属真·亚阈值轻拖/点按，逻辑正确(都 <commitRatio 0.30 且 <commitVelocity 400)；若手感觉得"钝"，可略调低 commitRatio/commitVelocity，非 bug。
- **代码侧提醒（非本日志触发，待 (16) 验）**：① 弹簧收尾在 `UIPercentDrivenInteractiveTransition` 驱动下，松手点可能有轻微"速度跳变"(手指线性→弹簧曲线)，纯观感、`弹性收尾` 可关；② 零位移快滑非交互分支(fast-swipe, triggered=0)仍依赖 iOS 自动收尾(旧路径，本日志未触发、旧已验过)，若该场景后冻结则疑此。
- **用户下一步**：装 `0.1.0+de9d0cd`、开调试日志、做几次边缘滑(含故意取消)，抓 (16) 发我 → 看是否出现 `animator done (spring=1 ...)`(提交) 与 `spring=0`/reduce(取消)，并查有无新异常。

## 分析 oback_debug(16).log（新包实测，用户问"有没有问题"）
- **总体健康，无问题**：全程无 abort/failed(无冻结)、无双返回(childCount 单步递减)、无崩溃；覆盖 com.tencent.xin(iConsole)、SHUIComponent 健康、im.xym.marknow、cn.runningquotient.RQ-Run 多 App，扩展进程(com.tencent.xin.sharetimeline)正确 skip。提交/取消判定全部符合阈值(动量投影正确提交小拖动)。
- **关键局限（重要）**：我原计划靠完成回调的 `animator done (spring=1)` 确认弹簧分支，但**该完成 block 的 OBLog 在交互转场流程里根本不打印**（(15)(16) 都没有；新旧包皆然——`completeTransition:` 由 `UIPercentDrivenInteractiveTransition` 内部收尾，完成 block 的 OBLog 不触发）。故**日志层面无法证弹簧**。确认新包是否装：设置里看「动画 / 弹性收尾」开关在不在；在=新包已装、弹簧默认开（用户在真机已在体验新动画）。
- **小观察（非 bug）**：① 18:20:07 一次手势 `indicator shown` 出现两次（shouldBegin+Began 各建一次胶囊，无害、瞬间替换）；② 18:21:33–18:21:47 SHUI 健康 App 连续 ~8 次左边缘取消（vel 多负/近0、percent 0.00–0.11），末 109 行大幅滑（percent0.75 vel1438）才成——用户在健康 App 里划得轻/反向，非 bug；若觉那 App 边缘返回钝，同前是 commitRatio/commitVelocity 灵敏度，可调。
- **给用户的选项**：日志不能证弹簧，要么去设置确认「弹性收尾」开关存在即新包生效；要么我可在 `animateTransition:` 开头加一行可靠日志（spring=1/0 + vel + dur，该处确定会执行），下份 (17) 铁证确认走了哪条分支。

## 已加 animateTransition 入口诊断日志（commit bddba5f，已 push，remote==local==bddba5f → CI 应出 0.1.0+bddba5f）
- 用户说"好的，加下" → 落实：在 `animateTransition:` 入口（确定会执行处）加 `OBLog(@"animator start (edge=%@ spring=%d reduce=%d vel=%.0f startP=%.2f dur=%.2f sv=%.2f)", ...)`。
- 同时把弹簧分支的 `dur`/`sv` 提到入口统一算（`_dur`/`_sv`），分支内复用，**行为完全不变**，仅新增日志。
- 下份日志 (17) 每条 `endTransition` 后应紧跟一条 `animator start`：spring=1=走了动量弹簧(默认)、spring=0=线性 easeOut 兜底(或开了减弱动态效果)、reduce=1=系统减弱动态效果触发兜底。据此即可铁证确认新动画分支。
- 注：旧的完成 block `animator done` 日志仍保留（交互转场里不触发，无害）。

## 分析 oback_debug(17).log（用户问"分析一下"）
- **头号发现（推翻上轮摘要假设）**：本日志**完全没有 bddba5f 新增的 `animator start` 入口诊断行**，也没有旧的 `animator done` 行。证明抓日志时设备跑的包**不含 bddba5f**——因为 `animateTransition:` 入口在每次交互转场（含 commit/cancel/abort）都必定执行，若装了 bddba5f 每条 endTransition 后必跟 `animator start`。零条=没装。故 (17) 与 (15)/(16) 日志结构完全相同，**无法证弹簧分支**。
- **推论**：(17) 至多含 5a5039e（弹性收尾动画升级，但该 commit 的 `animator done` 在完成 block 里不触发、日志不可见），肯定缺 bddba5f 诊断。需装 `0.1.0+bddba5f` 重抓 (18) 才看得到 `animator start (spring=1/0 reduce=...)`。
- **日志本体健康**（179 行）：
  - 覆盖 App：SHUI健康检查(nav-pop 双边缘)、微信 iConsole(nav-pop+modal 方案B)、marknow(仅挂载 linkNav=0)、babycare/duti(挂载 linkNav=0，shouldBegin=NO childCount=0 正确)、solos(nav-pop 双边缘)、扩展 com.tencent.xin.sharetimeline(start skipped 正确)。
  - **无冻结**：所有 `abortTransition (state=4)`=UIGestureRecognizerStateCancelled，是手势取消/被滚动抢占的安全清理（微信弹窗 6 次取消+2 次成功 commit），无卡死、无黑屏迹象。
  - **modal 方案B 安全**：微信弹窗 `beginTransition: dismiss modal (方案B...)` 成功 commit 2 次(percent0.57/vel651、percent0.43/vel624)，取消 6 次（含 1 次 triggered=0 快滑 vel=-265），全程只动 sheet、不碰 presenting，无黑屏。
  - **提交/取消阈值全对**：18:33:28 projected0.25<0.30 cancel 正确；18:34:54 projected0.58/vel578 commit 正确；18:35:06 percent0.52/vel1053 commit 正确；小拖动均 <阈值 cancel。
  - **无双返回**：childCount 每次 commit 单步递减（如 微信 3→2），实时补链 a62eded 生效。
  - **linkNav 计数 18→25 累积**：微信反复弹窗时每次 shouldBegin=YES 实时补链，视图树边缘手势数随时间增多（幂等、有界），非泄漏、非 bug。
  - **小观察**：marknow/babycare/duti linkNav=0（当时窗口无 nav 栈，正常）；微信 18:35:37 双 `indicator shown`（shouldBegin+Began 各建一次胶囊，瞬间替换无害）；babycare/duti shouldBegin=NO 因 childCount=0（无栈可返，正确）。
- **结论**：(17) 健康无 bug；唯一问题是没装 bddba5f 故不能验证弹簧分支。下一步=装 `0.1.0+bddba5f` 开调试日志重抓 (18)，看 `animator start` 是否 `spring=1`（默认走动量弹簧）及对应 vel/dur/sv。

## 代码核查：animator start 缺失 + 发现真正 bug（用户坚称装的是 0.1.0+bddba5f，让我查代码）
- **诊断行本身无 bug**：`OBLog(@"animator start ...")` 在 `ObackTransition.m:134`，位于 `animateTransition:` 入口（任何 early-return 之前）。同一套 `OBLog`（ObackManager.m:17，落文件+NSLog）。`animateTransition:` 是交互转场动画起点（UIKit 在 pop/dismiss 触发时调一次），动画 visibly 跑 ⇒ 必被调用 ⇒ 若 bddba5f 真装且调试日志开，`animator start` 必出现在日志。故 (17) 无此行 = **没装到 bddba5f**（装上前抓的 / .deb 是旧的）。验证：包管理器看版本号 0.1.0+bddba5f、重启 App 重抓 (18)。
- **🔴 真正 bug（读代码发现，比诊断行重要）**：弹簧**没继承松手速度**。
  - 时序：`beginTransition:`（ObackManager.m:339-340）先清零 `releaseVelocity/releasePercent` → `updateTransition` 首次横向拖动触发 `popViewControllerAnimated:` → **此刻 UIKit 调 `animateTransition:` 一次**，它去读 `releaseVelocity/releasePercent` → 但这俩值直到手势**结束**的 `endTransition:`（ObackManager.m:429-430）才写入。`finishInteractiveTransition` 只是续完 `animateTransition:` 已搭的动画，**不重调 `animateTransition:`**。所以 animateTransition 永远读到清零后的 **0/0**。
  - 后果：弹簧实际是 `initialSpringVelocity=0` + 按 `startP=0` 算的固定 `duration≈0.32s`、damping=0.82 的**固定弹簧**；"快甩更快归位"的动量继承**未生效**。肉眼只觉得弹，但速度没带入。
  - 铁证预测：真装 bddba5f 重抓后，每条 `animator start` 都会是 `vel=0.00 startP=0.00`——本身坐实 velocity 失效，也同时证明 bddba5f 已装。
- **修复方向（非平凡，动动画核心，待用户拍板）**：实现 `interruptibleAnimatorForTransition:` 返回 `UIViewPropertyAnimator`；在 `endTransition` 调 `finish` **之前**用真实 velocity 更新其 `timingParameters`（`continueAnimationWithTimingParameters:` + `UISpringTimingParameters` 带 `initialVelocity`），再 `finishInteractiveTransition`。仍走 `UIPercentDrivenInteractiveTransition`（冻结铁律不受影响）；需 ObackManager 在 finish 前能拿到当前 ObackAnimator 引用去改 timing。
- **次要（无害）**：`OBLog`（ObackManager.m:26-33）若日志文件被删，`fileHandleForWritingAtPath:` 返回 nil → 走 `[line writeToFile:atomically:]` 会**覆盖**首行；仅日志中途被删才触发，本次无关，可后续顺手加固（先 `createFileAtPath:` 保证存在再 append）。
