# 2026-07-21

## Oback 系统手势冲突规避（实施中）
- 用户确认：R2 走「跳过注入（稳定优先）」，执行顺序 R1→R3→R2。
- **R1 已完成**：ObackManager.m 的 `gestureRecognizerShouldBegin:` 将左边缘 scrollView 规避扩展为左右边缘通用（横向列表还能同方向滚时放行给滚动）。静态自检通过，未真机编译（Windows 环境无法 make）。
- **R3 已完成**：新增 ObackPanGestureRecognizer 子类（仅识别横向），touchesMoved 累计位移超 8pt 后判方向，纵向直接 Failed 不接管、放行底层滚动；attachToWindow 改用它（替代原任务里"在 shouldBegin 用 translation 判断"的不可行写法——begin 时位移为 0）。静态自检通过，未真机编译。
- 剩 R2（Tweak.xm `presentViewController:` 对 pageSheet/formSheet/popover 跳过转场注入，模态回系统原生 dismiss）。
- 注：真机验证需在 macOS+roothide Theos 编译后在相册/图片浏览器滑图确认不误触返回。
- **已推 GitHub 编译（2026-07-21）**：新机 `手势` 原非 git 仓库。用户改用方案3（HTTPS + 非一次性 GitHub PAT）推送，未走 SSH key 复制。流程：git init + remote(https://github.com/yxh41/Oback.git，无 token) + fetch(--depth 1，token URL) + reset FETCH_HEAD(保留远程历史) + commit(R1+R3，仅 ObackManager.m 48插6删) + push(5043f70..3ac05d5 main)。remote URL 确认不含 token。GitHub Actions(build.yml) 应已自动编译 roothide .deb。R2(模态转场过滤) 仍未做，用户暂缓。
