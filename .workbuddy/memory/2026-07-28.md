# 2026-07-28 Oback 工作日志

## 从 GitHub 同步最新进度到本地
- 本地 main `5881738` → `9bf479e`（fast-forward，无冲突）。origin/main 此前领先 11 个提交。
- 新增本地跟踪分支 `feat/navbar-coordination`（f63952b：胶囊 6 特效 + 导航栏快照/还原实验）。
- 工作树干净。

### 本次同步带回的关键进展（来自 2026-07-25~26 的远端提交）
- **朋友圈漏挂/不返回已正面修复**：swizzle `UINavigationController` 的 `viewDidLoad/viewDidAppear/viewDidLayoutSubviews` 三 hook，任意子类（含微信 MMUINavigationController）加载即挂边缘 pan 到 nav.view；pan 所属 nav 绑 `kObackNavKey`，`shouldBegin` 直接读 pan 所属 nav，不再依赖 topMost 标准链枚举。链接移出每次手势热路径（防起点卡顿）。
- **Filza 冻结兜底**：nav pop 安全看门狗 + interacting 守卫，根治系统交互转场卡死。
- **触发更稳**：放松"仅横向"判定；排除微信朋友圈恢复原生边缘返回修右侧"划很多次"。
- **导航视差实验开关（默认关）**：`navParallaxEnabled` + 调试日志 plist 默认关。但 `a105723`（视差改纯自定义单驱动）已被 `9bf479e` revert 回 `6c8aefe` 稳定基线——该重构等于回到 60352e6 被证伪方案，会重引"导航栏损坏"。
- **marknow 打不开**：已实锤与 oback 无关（Flutter/越狱环境兼容），黑名单跳过挂载后仍不开。

### 当前真实战场（基于 9bf479e）
- main 是稳定基线；朋友圈修复在代码层已落地，待用户装最新构建真机验证（列表/详情边缘返回、点进详情、其它 app 返回/dismiss、冻结是否消失）。
- `feat/navbar-coordination` 在做导航栏协同（胶囊特效 + 快照/还原引擎），未合 main，建议导航栏协同攻坚前不在 main 开实验开关。

### 协作教训（重申）
- 跨机器协作凡结论依赖"某 commit/某串是否存在"，务必先 `git fetch` 再查；stale 的 origin/main 本地引用会导出相反错误结论。

## 胶囊特效优化（feat/navbar-coordination，commit f53d8be）
- 用户反馈：胶囊风格里的「流光渐变」「霓虹」太生硬。两特效仅在 feat 分支 ObackManager.m 的 ObackEdgeIndicator init switch 里。
- **流光渐变（ObackCapsuleEffectGradient=3）生硬根因**：旧版同时动画 `startPoint`(0→1) 与 `endPoint`(1→0) 并 `autoreverses=YES` → 高亮带扫过去又硬倒回。改为 **2 倍宽渐变层（含两个相同周期）+ 单向平移一个周期(w)**，`fromValue=0→toValue=-w`、linear、无限循环；平移一个周期后首尾完全一致 → 无缝连续流动，无回弹/跳变。设 `self.layer.masksToBounds=YES` 裁剪到圆角（`transform.translation.x` 平移，`removeAnimationForKey` 同步停）。
- **霓虹（ObackCapsuleEffectNeon=2）生硬根因**：2pt 纯色硬描边 + 恒定强晕，像贴纸。改为 **近白青亮核(1.5pt) + 更大半径(22)半透明柔晕 + shadowOpacity 0.55~0.95 ease 微呼吸脉冲(2.6s autoreverse)**，模拟真实灯管明灭。`stopEffectAnimations` 同步 `removeAnimationForKey:@"obNeonPulse"` 防淡出残留。
- MRC 合规（无 __weak），无旧 obFlowStart/obFlowEnd 残留。已 push `f53d8be`。
- 注：此改动在 feat 分支，不在 main；用户装 feat 分支构建才会看到效果。待真机视觉验证自然度。

## 左右缘差异全面排查（本回合，用户问：右缘能触发微信朋友圈返回、左缘"识别切换"后仍不行）
- 架构：左缘 = nav pop，走方案A 驱动系统私有 `_UINavigationInteractiveTransition`（跟手）；右缘 = 非交互 `popViewControllerAnimated:` 松手提交（rightSimplePop）；modal dismiss 另走方案B（只移 sheet）。
- 微信不对称根因：左缘 `beginTransition` 调 `driveSystemNavPopBegin` → 内含 `_navPopShouldDriveSystemNav`（com.tencent.xin / `MMUI*` 返回 NO）→ 方案A 关闭；右缘被 `beginTransition` 的 `edge != ObackEdgeRight` 挡在 `driveSystemNavPopBegin` 之外，根本不进微信识别 → 免疫，永远走 rightSimplePop。这就是"右缘正常、左缘不行"的本质。
- 当前 main 代码左缘在微信已有运行时兜底：`driveSystemNavPopBegin` 对微信置 `_navPopProbeFailed=YES`，`updateTransition` 首次横拖(line 967)直接 `popViewControllerAnimated:`（非交互，首微拖即弹，不跟手）。
- 结论：若用户测的是旧构建（兜底落地前）左缘真不返回；新构建左缘应能返回但非交互 + 依赖脆弱探测。建议统一修复：左缘遇 `_navPopShouldDriveSystemNav=NO` 时改设 `rightSimplePop=YES`（与右缘一致的松手提交），消除脆弱探测、让灵敏度滑块也对左缘微信生效。

## 整支 feat 合并到 main 触发 CI 出包（本回合）
- 用户选择整支 `feat/navbar-coordination` 合并进 `main`（含未真机验证的导航栏快照/还原实验代码），自行承担风险。
- 合并产生 merge commit 并 push `origin/main`，GitHub Actions 将出 roothide `.deb`（版本 `0.1.0+<hash>`）。
- 待真机验证：① 胶囊自然度（流光/霓虹）；② 实验性导航栏代码是否引入回归（手势/冻结/导航栏损坏）。

## 左缘微信统一为 rightSimplePop（commit 6fb9fe0，已 push main 触发 CI）
- 采纳排查结论的"统一修复"：在 `gestureRecognizerShouldBegin:` 左缘分支直接判定
  `_navPopShouldDriveSystemNav:` 对微信/com.tencent.xin、`MMUI*` 自定义 nav 改设
  `currentParallaxToView=NO` + `rightSimplePop=YES`，复用右缘松手提交机制。
- 效果：左缘微信与右缘行为完全一致（非交互、受灵敏度滑块控制），消除对脆弱运行时探测
  （`_navPopProbeFailed` 首微拖即弹）的依赖；标准 nav 左缘仍走方案A 跟手，不受影响。
- 已确认安全：`rightSimplePop` 在 `endTransition`/`updateTransition` 均为左右缘通用（靠 currentEdge 算方向），
  且 `updateTransition` 的 rightSimplePop 分支提前 return 不调 `triggerTransitionInWindow`；
  `_navPopShouldDriveSystemNav:` 纯判定无副作用，可在 shouldBegin 调。MRC 合规（无 __weak）。
- 版本：`0.1.0+6fb9fe0`。待真机验证左缘微信朋友圈返回手感（应与右缘一致：松手提交、跟灵敏度滑块）。

## 流光渐变柔化（commit e08bc6e，已 push main 触发 CI）
- 用户反馈：胶囊「流光渐变」仍生硬，"两个颜色的界线不够和谐"。
- 根因定位：非拼接缝问题（拼接数学无缝），而是 **近白高光(0.92,0.97,1.0)贴饱和蓝(0.30,0.66,1.0)
  对比过强 → 扫过成生硬亮带**；且高光峰是 5-stop 三角尖峰，转折硬。
- 修复：① 高光收回同色系浅蓝(0.60,0.84,1.0)，与基色(0.18,0.50,0.95)均在蓝家族 → 蓝白硬界线消失；
  ② 用 9 个细分 stop(基,中,高,中,基 ×2)把尖峰揉成圆润钟形，边界天然柔化；③ 流速 3.2→4.2s 更宁静。
- 版本：`0.1.0+e08bc6e`。待真机看流光自然度；若仍偏"亮带感"可再降对比或加多周期(3~4 个柔光峰)。

## 流光渐变改为细碎流光（commit 50dcc11，已 push main 触发 CI）
- 用户明确要"细碎流光"。把单条大亮带换成多个窄而柔的高光峰连续流动。
- 渐变层仍 2 倍宽双周期、平移 w 无缝；每周期排 4 个窄柔峰（全层 8 个），峰间用基色留缝。
- 33 个 stop（基,中,高,中,基 重复成 8 峰）把每峰揉圆；同色系低对比保留（无蓝白硬界线）；流速 4.0s。
- colors/locations 均 33 项匹配（已 grep 精确核对；locations 数曾被正则误抓霓虹 @0.55/@0.95，实际 33）。
- 版本：`0.1.0+50dcc11`。待真机看细碎流光密度/亮度；若太密可减到 3 峰/周期，太淡可提 cHi 亮度。

## 左缘微信朋友圈仍不返回——根因在 shouldRequireFailureOf 让步（commit 678169d，已 push main）
- 用户实测：6fb9fe0 的"左缘改 rightSimplePop"在微信朋友圈仍未返回。重读链：左缘 rightSimplePop 设置方向对，但手势在 shouldBegin 之后被 `shouldRequireFailureOf:` 取消，rightSimplePop 逻辑从未执行。
- 真正根因：`shouldRequireFailureOf:` 对左缘写死 `return YES`（向同边对手左边缘手势让步）。微信朋友圈有**自带的左边缘返回手势**(UIScreenEdgePanGestureRecognizer，非被 717 禁用的系统 interactivePop)。该手势在朋友圈能识别→我们的左缘被判"需等对手失败"→对手成功→Oback 左缘被 Cancelled→abortTransition 清理、不返回；而微信该手势在自定义容器层级下识别了却不真正返回→双输。右缘由该处 `return NO` 从不让步，故右缘稳。这才是左右不对称的最终根因。
- 修复：`shouldRequireFailureOf:` 中，当 g 为左缘且所属 nav 为"我们接管型"(`_navPopShouldDriveSystemNav=NO`，微信类)时 `return NO` 不让步，由 Oback 左缘 rightSimplePop 独占接管（与右缘一致）。标准 nav 保留原让步（防护系统/插件双返回）。新加诊断日志。
- 风险：对微信类 nav 不让步后，若微信自带左边缘手势在个别页也真正返回，理论上可能双返回。当前痛点(不返回)优先，真机若见双返回再补"主动让对手失败于我们的 pan"。
- 版本：`0.1.0+678169d`。待真机验证微信左缘朋友圈返回（应同右缘：松手提交、跟灵敏度滑块）。

## oback_debug(58).log 分析——678169d 的"不让步"分支仍不命中（commit 5624207，已 push main）
- 用户提供 `oback_debug(58).log` 验证 678169d 是否生效。日志铁证：
  - 左缘(微信朋友圈 WCTimeLineViewController)：多次 `shouldBegin=YES` + `indicator shown`，但**从无 `beginTransition: entered`**（对比右缘有 beginTransition 且 `endTransition 非交互 pop commit=1` 成功返回）。
  - 全日志**完全无** `shouldRequireFailure: 左缘接管型nav...不让步` 诊断日志。
  - 说明左缘 pan 在 `Began` 前被系统 `Cancelled`（典型 shouldRequireFailureOf 让步后果）；且不让步分支根本没命中。
- 深挖 678169d 失效根因：其 `shouldRequireFailureOf:` 用 `objc_getAssociatedObject(mg, kObackNavKey)` 取"左缘 pan 所属 nav"判定接管型。但 `kObackNavKey` 在 `_attachNavPanToNav` 以 `OBJC_ASSOCIATION_ASSIGN` set 到 pan；**微信 nav pan 经 swizzle 自动挂载路径未必可靠绑定该 key**（或 assign 时机），导致 `gNav` 取空 → `_navPopShouldDriveSystemNav` 判定永不进 → 落到 `return YES` 让步。注意：右缘 endTransition 能从 pan 取到 nav 是因 pop 时 pan 仍存活、key 尚在；但不让步判定发生在识别竞争早期，key 可能未就绪/未绑。
- 修复（5624207）：不再依赖 pan 关联对象，改在 **`gestureRecognizerShouldBegin:` 左缘分支**（此处已可靠拿到 nav 并完成 `_navPopShouldDriveSystemNav` 判定）设 ivar `_leftEdgeTakeoverActive`，每次手势判定先重置、左缘接管型置位；`shouldRequireFailureOf` 左缘分支直接读该 ivar 决定不让步。彻底绕开绑定时机不确定性，且 shouldBegin 必先于 shouldRequireFailureOf 调用，时序天然正确。
- MRC 合规（无 __weak/weak 属性）；mg/og 在 949/953 行仍使用无 unused 警告。
- 版本：`0.1.0+5624207`。**这是左缘微信不返回的第 N 次根因收敛，关键证据链已闭环**；待真机验证微信左缘朋友圈现在能返回（抓一次 log 应能看到 `shouldRequireFailure: 左缘接管型nav不让步` + 左缘 beginTransition + endTransition 非交互 pop commit=1）。

## 流光渐变再压对比（commit eeac2c0，已 push main）
- 用户仍反馈"白色跟蓝色对比太强烈、过渡生硬"。此前 50dcc11 已把高光从近白(0.92,0.97,1.0)改浅蓝(0.62,0.85,1.0)，但浅蓝高光配饱和深蓝基色(0.18,0.50,0.95)明度+饱和度双差叠加，视觉仍是"亮块扫暗底"。
- 改动：基色提亮为偏亮蓝(0.30,0.58,0.98)；高光压到仅略亮的蓝(0.53,0.78,1.0)；中蓝(0.42,0.68,1.0)。基↔高差距 R:0.44/G:0.35 → R:0.23/G:0.20（砍半），整体成"同一蓝明度微妙起伏"，高光绝非白。流速 4.0→4.6s 更柔。保持 33 stop 双周期无缝结构只调色值。
- 版本：`0.1.0+eeac2c0`。待真机看自然度；**若仍觉"碎亮点"刺眼（对比已压，剩下是峰太窄太碎问题），下一步把峰数 4/周期减到 2/周期、每峰更宽更淡**（需重算 locations 保持无缝，风险可控）。
- 历史回溯：用户问"这个方式以前有没有用过"——`shouldRequireFailureOf:` 单向让步机制自 2026-07-26(894346d) 就在用（右缘稳靠它）；但"按 nav 类型动态让左缘打破让步"是 678169d 首次引入、5624207 修正实现。早期(6614322)曾用 `requireGestureRecognizerToFail:` 反向让系统 interactivePop 失败于我们的 pan，后(894346d)移除改用本机制。

## 流光渐变改为舒缓流光（commit 3d697e8，已 push main）
- 用户确认对比已顺，但"碎亮点"刺眼——按 86 行既定预案执行：4 峰/周期 → 2 峰/周期（全层 8→4 个宽柔峰），峰宽 0.125→0.25 翻倍、峰间留更宽 base 缝；33→17 个 stop（基,中,高,中,基 ×2 周期，colors/locations 均 17 匹配、0.0/0.5/1.0 均 base → 平移 w 无缝）；流速 4.6→5.5s 更慢更宽。同色系低对比三色保留（基 0.30,0.58,0.98/中 0.42,0.68,1.0/高 0.53,0.78,1.0）。MRC 合规。
- 版本：`0.1.0+3d697e8`。待真机看舒缓流光自然度；若仍偏明显可再降高光亮度或进一步放慢。

## oback_debug(59).log + 左缘不让步顺序竞态（根因级修复，commit 5caecd1，已 push main）
- 用户报"微信朋友圈左缘还是无法返回"。读 59.log 发现**关键反直觉事实**：5624207 的修复其实生效了——
  日志第 5 行 `shouldRequireFailure: 左缘接管型nav(StatusBarMonitorNavigationController)不让步` + 第 7~12 行
  微信聊天内容页(BaseMsgContentViewController)左缘 `shouldBegin=YES→beginTransition→endTransition 非交互 pop commit=1` **成功返回**。
  但**整份日志无 WCTimeLineViewController（朋友圈）任何一行**——朋友圈左缘根本没走到 shouldBegin。
- **真正根因（59.log 顺序铁证）**：第 5 行 `shouldRequireFailureOf` 排在**第 7 行 `gestureRecognizerShouldBegin` 之前**。
  旧方案(5624207)靠 shouldBegin 设置的 ivar `_leftEdgeTakeoverActive` 判定"不让步"，但 shouldRequireFailureOf
  被调用时该 ivar 尚未设置、读到上一次手势残留值(或 NO)→ 落到 `return YES` 让步 → 左缘被微信自带左边缘
  手势取消 → 不返回。聊天内容页那次仅因调用顺序碰巧正确才成功；朋友圈顺序一反即失效（同 nav、同代码，纯顺序问题）。
- **修复**：彻底移除 `_leftEdgeTakeoverActive` ivar 及全部引用；改为在 `shouldRequireFailureOf` 里**直接从
  `mg.view.nextResponder` 解析 pan 所属 nav**（nav.view.nextResponder 即 UINavigationController，零关联对象
  时机/顺序问题），现场调 `_navPopShouldDriveSystemNav:` 判定接管型→不让步。window modal pan 的 mg.view 为
  UIWindow(nextResponder=UIApplication 非 nav)→mnav=nil→正常让步，不受影响。
- **诊断增强**：shouldRequireFailureOf 左缘冲突日志加 opponent 视图类+ourNav+takeover；shouldBegin nav 分支加
  `[diag-left-nav]`(nav 类/top 类/presenting/childCount)——下次抓朋友圈现场即使它早退也能看到。
- 版本：`0.1.0+5caecd1`。**待真机验证朋友圈左缘**：抓一次 log 应见 `shouldRequireFailure: 左缘冲突 ... ourNav=StatusBarMonitorNavigationController takeover=1` + `[diag-left-nav] ... top=WCTimeLineViewController` + 左缘 beginTransition + endTransition 非交互 pop commit=1。若仍无 WCTimeLineViewController 行，说明朋友圈根本没到达我们的 pan（可能被排除名单/未在 swizzled nav 上/或以 modal 呈现导致 nav↔modal 双退避死锁，需凭新诊断定位）。

## 诊断日志机制澄清（用户疑"另一插件冲突导致没日志"）
- **30min 自动过期已移除**：`ObackPreferences.m:164-165` 明确"不再做自动过期写回"。故"突然没日志"不是过期，而是调试开关被手动关(默认关，`debugLogEnabled` 第18行直接 return) 或 路径回退沙盒。
- **OBLog 写入是追加模式**：`ObackManager.m:26-30` 用 `fileHandleForWritingAtPath:` + `seekToEndOfFile` + `writeData`，不会自己清自己。
- **syslog 双通道、不受文件冲突影响**：`OBLog` 第35行 `NSLog` 让每条日志同时进系统 syslog；且 Tweak.xm:264 `[Oback] 已注入...` 不受调试开关门控（可确认 tweak 是否加载）。**结论：用 syslog 实时抓 `Oback` 比依赖文件更稳**，可绕过"别的插件覆盖同名文件"或"路径回退沙盒"两类问题。
- **撞名风险**：文件名 `/var/mobile/oback_debug.log` 太通用，若别的越狱插件也写同名且用覆盖模式(writeToFile/fopen "w")会清掉我们的内容 → 表现为"日志时有时无"。**根治：改成专属文件名（如 `oback_gesture.log`）**，1~2 行低风险改动，下次构建一并带。
- 给用户的排查顺序建议：① 设置确认「调试日志」开关开 → ② 用 syslog(`idevicesyslog|grep Oback` 或 CrashReporter/TrollStore console)实时抓 → ③ 再考虑改专属文件名重新出包。

## 代码全面审查 + 安全精简（本回合）
- 用户要求全面检查代码、优化精简但**不改变已稳定功能**。审查范围 main 分支；核心手势判定链(shouldBegin/shouldRequireFailureOf/begin/update/endTransition/driveSystemNavPopBegin/_navPopShouldDriveSystemNav 等)、胶囊特效、诊断日志 OBLog 列为「禁改区」（多轮真机调试收敛，看似冗余实则有意，改则引入回归风险）。
- 审查手段：Grep 全仓库方法定义/调用交叉验证 + 读 ivar 区与注释。结论：
  - **无未使用的私有方法**（_diagLogEdgeGesturesInWindow/_enumerate*/topMost/_windowForPan/_isExcluded* 等逐一确认全在调用）。
  - **无遗留旧 ivar**（_leftEdgeTakeoverActive 已彻底清除，Grep 0 引用）。
  - 大量「旧版/为什么这样/历史修复原因」注释属有效设计说明，删了丢上下文，**保留**。
  - ObackManager.m 单文件 1400+ 行，拆分属大规模重构，违反「不改变稳定功能」，**不做**。
  - 诊断日志 OBLog 及密集调用属调试功能，精简会削弱后续诊断能力，**不动**。
- **唯一明确安全精简项（已 Edit 落地 ObackPreferences.m，尚未 git 提交——本会话 Bash 工具持续报 command 参数解析错误，Grep/Read/Edit 正常，无法 commit/push）**：
  - 删 `static NSTimeInterval __obDebugLogOpenedAt = 0;`（152）、`#define OB_DEBUG_LOG_EXPIRE 1800.0`（154）、及其赋值行（166）：移除「30min 自动过期写回」后已无任何引用（Grep 全仓库 0 引用），纯死代码。
  - 修正顶部过时注释（原「开启后 30 分钟自动过期写回关闭」与现状矛盾——正是此前误判「日志自动过期」的根源）→ 改「开启后常驻开，需手动关闭」。
  - 零行为变化、MRC 合规（仅删未使用静态变量/宏 + 注释勘误）。
- **待办**：待 Bash 工具恢复或用户手动提交后，再 push main 触发 CI（此改动太小无需单独出包验证，可攒批）。
