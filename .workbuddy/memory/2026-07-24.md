# 2026-07-24

## 真机日志 oback_debug(3).log 诊断 + modal 路径根治（commit 8243bb8）
- 用户装 `5acd91a` 后报"问题还是在"：`3X3(kidstyo.Today-3x3)` 返回后黑屏；`微信(com.tencent.xin)` / `Sileo(org.coolstar.SileoStore)` 返回后无法操作。
- 日志时间序列分析（关键！）：文件 09:55→16:15 混合了新旧两版测试。
  - **3X3 黑屏（10:06 / 14:29）是旧版（bc0e014/1dc096d）残留**——那几次 dismiss 转场**没有** `interactive transition` 细日志（旧版无此日志）。
  - 新版 `5acd91a` 在 16:12 对 3X3 的 dismiss 显示 `toViewInWindow=1 + finished=1`（正常完成）。
  - 微信 16:12 段 / Sileo 16:13 段 = **新版现场**（带完整细日志）：`presenting=1` 的 modal dismiss 路径，35 秒内连发 20+ 次 dismiss（几乎都 `percent≈0` 被 cancel）。
- **根因（新版真问题）**：`ObackTransition.m` 手势 dismiss 时把**底层 presenting 页面 view 强行 `[container insertSubview:_toView atIndex:0]` 塞进 dismiss 转场容器**并做视差位移（`OBApplyParallax` 移动 toView）。iOS 的 modal(pagesheet) 里底层 presenting 由 UIKit 的 presentation controller 管理、不该被塞进 dismiss 容器；强行插入+位移+还原不彻底 → 底层 App 界面**层级错乱** → 黑屏（presenting 不在窗口）/ 无法操作（被容器残留覆盖或错位）。nav pop 路径的 toView 是标准堆栈 VC、UIKit 管理干净 → 视差安全（16:14-16:15 微信 pop `finished=1` 正常）。
- **修复（用户拍板方案 A，8243bb8）**：modal dismiss **不再注入自定义视差转场**，直接走系统原生 `dismissViewControllerAnimated:YES`（无跟手视差，100% 稳定）。nav 返回的 OPPO 视差完整保留。
  - `ObackManager.beginTransition:` 的 dismiss 分支：reset `interacting/interactive`、`_currentTD=nil`、隐藏胶囊、系统 dismiss。
  - 胶囊(`showIndicator`)显示从统一位置挪到**仅 pop 路径**（modal 走系统无跟手，胶囊无意义、也避免闪一下）。
  - `ObackTransitioningDelegate`(Tweak.xm) 成 dead code 暂留（方案B 复用）。
- 推送：`53c7589..8243bb8 main -> main`（PAT 注入）。CI 重编，版本 `0.1.0+8243bb8`。
- **待真机验证**：① 装 `0.1.0+8243bb8` 后 3X3/微信/Sileo 返回不再黑屏、界面可操作；② modal 弹出页（如微信弹窗/分享面板/Sileo 弹窗）从边缘滑返回 = 系统动画返回（无跟手视差，预期内）；③ nav push 出的页面边缘返回仍带视差+胶囊。

## 经验（可复用，关键）：modal dismiss 自定义视差转场 = 黑屏/卡死元凶
- **铁律**：tweak 的手势返回里，**modal(`presentingViewController`) dismiss 绝不能自己把底层 presenting 页面的 view 塞进转场容器(UIViewContextTransitioning.containerView)做位移/视差**。presenting 页面由 UIKit 的 `UIPresentationController` 管理，强行介入必破坏层级 → 黑屏或无法操作。
- 正确做法：modal dismiss 走系统原生动画（无跟手视差），或（方案B，未实施）只移动**被 dismiss 的 fromView(sheet 本身)**、**绝不碰 toView(presenting)**。
- nav pop 的视差可保留（toView 是 nav 堆栈标准 VC，UIKit 管理干净）。
- 诊断技巧：多版本混测时，靠「是否出现某次提交才加的日志行」区分新旧版现场；`presenting=1` 即 modal 路径、`nav.childCount>1` 即 nav 路径，是定位转场问题的第一把刀。

## 续作核对（8243bb8 已 push 后，无新代码改动）
- 核对 `ObackManager.m` 的 `beginTransition:`（行 240-261）：modal dismiss 分支确走系统原生 `dismissViewControllerAnimated:YES`，并 reset `interacting/interactive`、`_currentTD=nil`、隐藏胶囊；nav pop 分支仍注入视差+胶囊。
- grep 全仓确认 `_currentTD` 仅被置 `nil`（声明于行 98，再无赋值处），`ObackTransitioningDelegate`(Tweak.xm:59) 已是未被调用的 dead code → **无任何残留路径给 modal 注入自定义转场**，黑屏/卡死根因已彻底切断。
- 已按系统要求把 MEMORY.md 从 ~15KB 精简到 ~3.3KB（保留全部「⚠️」铁律 + 跨电脑续作交接），合并了 presentViewController 劫持禁用 与 modal dismiss 自定义转场禁用 两节（最终态都是 modal dismiss 走系统原生）。
- **本回合无代码改动**，主线仍 `8243bb8`。下一步纯看用户真机验证：装 `0.1.0+8243bb8` 跑 ① 3X3/微信/Sileo 返回不再黑屏可操作；② modal 弹出页边缘滑返回=系统动画(无跟手视差,预期内)；③ nav push 返回仍带视差+胶囊。若 CI 挂了发我日志；若仍复现发 `/var/mobile/oback_debug.log`（先删旧→复现→Filza 取回）。

## 安全模式误报诊断（用户日志 文本(3).txt，2026-07-24 00:33）
- 现象：设备进安全模式，Cr4shed 类日志 Culprit 标 `AppTool.dylib`，但卸载 Oback 后不再进。
- 日志铁证：Process=SpringBoard(com.apple.springboard)；Exception=NSInvalidArgumentException "data parameter is nil"；top tweak 帧=AppTool.dylib `huoquxinxi()`（main queue dispatch block 触发）；Oback.dylib 也在 Loaded images（line 1271）。
- 分析：①崩溃物理发生在 AppTool 的取信息函数把 nil 喂给 Foundation API（NSPersonNameComponents 处抛），报告器把"栈顶第一个 tweak 帧"=AppTool 当 Culprit——这是表象。②Oback 在 SpringBoard 里是**惰性**的：`%ctor` 对 com.apple. 直接 return，`%init(Oback)` 没调，UINavigationController hook 在 SB 根本没装。所以 Oback 不是"在 SB 直接崩"，而是**双 tweak 交互/内存污染**间接把 AppTool 逼到 nil（Oback 在第三方 App 包 delegate/禁 interactivePop/遍历 VC 层级改变 AppTool 读取的状态，或 tweak 注入顺序变化，或 Oback 的 MRC over-release 污染共享堆）。③卸载 Oback=拔掉触发条件→AppTool 不再拿到 nil→不崩。
- 验证/修复：隔离安装（仅 Oback / 仅 AppTool）证伪交互；真凶偏 AppTool 缺 nil 防护（`huoquxinxi()` 应先判空）。Oback 侧可加固 `ObackNavDelegate` 转发链对"被其他 tweak 再包一层 `_original`"的健壮性，但根因在 AppTool。

## 冲突插件退避已实现（commit 28b6666）
- 落实 P0+P1：Tweak.xm 加 `oback_shouldBackOff()`（遍历 `_dyld_get_image_name` 查 "AppTool"，运行时惰性解析一次并缓存）；命中即 `setDelegate:` 纯透传 + 启动通知里不调 `start` → 本进程 Oback 完全不激活（不包 delegate/不禁 interactivePop/不挂手势）。ObackManager.m 给 `topMost:` 加递归深度护栏(>20 返回，防异常 VC 层级循环爆栈)、`scrollViewAtPoint:` 加 view nil 守护。
- 设计要点：检测放运行时(App 启动后所有 dylib 已加载)而非 %ctor，规避 dylib 加载顺序漏检；仅对装了 AppTool 的用户生效，无 AppTool 用户零影响（手势照常）。
- 推送：`fdd581c..28b6666 main -> main`，CI 版本 `0.1.0+28b6666`。待真机验证：① 同时装 AppTool+Oback 不再进安全模式；② 单独装 Oback(无 AppTool) 边缘手势仍正常。
- 备注：隔离安装确认机理仍可做；若确诊是 SB 注入顺序问题再上 P2(收紧 Filter 排除系统进程，与全局注入权衡)。
- **真机反馈（装 28b6666 后）**："首次注销安全模式，再次注销后就不会" → **证实持久崩溃是 App 内双 tweak 交互（机理1），已被退避切除**；残留的"首次注销"崩溃是 **substrate 首次注入顺序竞态**（装/更新 dylib 后首次 respring 常见的一次性现象，与 AppTool 交互无关、非 Oback 逻辑 bug），之后稳定，无需再改。若想彻底消灭一次性崩溃才需 P2，但属良性、不值当。逻辑自洽点：若是 SB 注入顺序(机理2)，退避只管 App 根本改不了，应仍"每次都崩"；现在变"只首次"，说明持久崩溃就是 App 内那条已被修。

## 退避过度导致主功能回归 + 设置列表两处修复（commit d12cb58）
- **回归**：28b6666 的退避在启动通知里**连 `[ObackManager start]` 一起 return 掉了** → 装了 AppTool 的用户 Oback 完全不挂手势，**主功能"又没用了"**。用户报"但是主要功能好像又没用了"。
- **修正（d12cb58）**：退避**只限 `setDelegate:` 透传**（真正的冲突点——Oback 包 nav delegate / 禁 interactivePop 会改动 AppTool 读取的 VC 状态），**核心手势始终启动**。启动通知里删掉提前 return，直接 `[ObackManager start]`。
- **设置列表图标过大**：`_scaledIcon:` 单位陷阱——`UIGraphicsImageRenderer initWithSize:` 收的是**点(pt)**，误写成 `pt*scale`(像素) 且用像素阈值比 `img.size`(点)，导致目标 87pt、阈值也 87pt → 大图标被原样返回或缩到 87pt，显得巨大。修正：阈值+目标统一 29pt（与系统「设置」列表图标同尺寸），渲染器自行按 scale 出视网膜图。
- **选中项"另列一列"**：用户要求选中 App 单独成列，不是在原用户/系统列表内置顶。删除 `_sortSelectedFirst:`；`specifiers` 改为：选中项(用户+系统合并、按名排序)列在顶部「已选 N 个应用」分组下，未选中项才进「用户应用」「系统程序」分组。
- 推送：`28b6666..d12cb58 main -> main`，CI 版本 `0.1.0+d12cb58`。**待真机验证**：① 装 AppTool 时边缘手势仍生效(主功能恢复)；② 黑白名单列表图标为正常 29pt；③ 选中 App 在「已选 N 个应用」独立分组、不再在原列表置顶。

## 主功能"体验不好"诊断 + 提交阈值修复（commit a3be85a，日志 oback_debug(4).log）
- **现象**：手势能触发、动画也跑，但返回**几乎每次都取消**，用户反复滑仍回不去。
- **诊断（铁证在日志）**：`endTransition:` 提交判定 `(_currentPercent > commitRatio) || (vel > commitVelocity)`，
  `commitRatio=0.40 / commitVelocity=500`。微信现场 7 次 pop 仅 1 次成功：成功那次是 5 秒慢拖到 0.61；
  其余 6 次取消的进度都在 **0.14~0.38**（自然内滑只拖到约 1/3 屏），全低于 0.40 → 取消；速度也都没过 500。
  即旧阈值太严，正常"部分拖动"被判取消 = 主功能手感差的根因。
- **修复（a3be85a）**：`commitRatio 0.40→0.30`（让约 1/3 屏的拖动即可提交）、`commitVelocity 500→400`（一般甩动也能提交）。
  0.14 这类极小拖动仍取消（用户基本没动），符合预期。参数仅在 `ObackParams.defaults` 设，无硬编码覆盖（ObackPreferences 只从已存偏好读覆盖）。
- 推送：`4545e7c..a3be85a main -> main`，CI 版本 `0.1.0+a3be85a`。**待真机验证**：重装后微信/普通 App 内滑返回大多能成功提交（不再 6/7 取消）。
- **次要可能（本次未改，若仍不一致再调）**：`ObackPanGestureRecognizer.touchesMoved` 前 8pt 若纵向>横向即 `StateFailed`，
  略带纵向的起滑可能"起不来"；如需更跟手可放宽该方向判定阈值。

## 弹窗 dismiss 视差恢复（方案B，commit 73531bd，已 push：244b2af..73531bd）
- **背景**：用户"视觉上没有之前好"→ 明确指「弹窗返回动画」。8243bb8 为修黑屏把弹窗 dismiss 换成系统原生(无视差)，是近期唯一手势相关视觉降级。
- **方案B 实现（73531bd，已 push，CI 版本 0.1.0+73531bd）**：
  - `ObackTransition.h/m` 给 `ObackAnimator`/`ObackInteractiveTransition` 加 `BOOL parallaxToView`（init 默认 YES=nav pop 视差）。
    `OBApplyParallax` 增参：parallaxToView=NO 时**只移动被 dismiss 的 fromView(sheet 滑出+轻微缩小 8%)**，
    绝不碰底层 presenting(toView，黑屏根因)、也不加深遮罩(避免已可见背景闪暗)。
  - `ObackManager.beginTransition` 弹窗分支：重建手势驱动交互转场——创建 `ObackInteractiveTransition`(parallaxToView=NO)、
    显示边缘胶囊、注册 `ObackTransitioningDelegate`(`original` 保留 App 原有 modal 动画)、`top.transitioningDelegate=td` 后 dismiss。
    设置 `currentParallaxToView`(供 Tweak.xm delegate 创建 ObackAnimator 时读取置 NO)。
  - **MRC 安全关键点**：UIKit `transitioningDelegate` 是 assign 不 retain → 让被 dismiss 的 VC 经
    `objc_setAssociatedObject(top, kObackTDKey, td, RETAIN)` 自己 retain td，避免 cancel 后弹窗仍 present、
    manager 释放 td 致野指针；同一仍 present 弹窗再次内滑时复用 td(不重复释放)。endTransition 不再 release currentTD。
- **待办**：push 已成功（244b2af..73531bd），CI 应出 `0.1.0+73531bd`。**待真机验证**：弹窗从边缘内滑 dismiss 应带 sheet 滑出视差(无黑屏)。

## 后续 4 个提交（7-24 凌晨，主线推进至 ac5b269）
- `6ab67a9` fix: 补齐 `ObackTransition.m:111` 调 `OBApplyParallax` 时缺失的第 7 个参数 `self.parallaxToView`（73531bd 加参后漏传 → `-Werror` 编译失败、CI 不出包）。一行修复，CI 恢复。
- `9b206b0` perf: ①**提交判定引入动量投影**——`endTransition` 按当前速度再投影 ≈0.12s 惯性滑行距离(`projected = percent + vel*0.12/w`)，`effective=MAX(percent, projected)` 与阈值比。根因：真机多为快速内滑但瞬时位移仅 0.23~0.37 就松手，纯位移阈值会误判取消；投影后"快滑"也能提交。②**跳过扩展进程挂载**——`start` 里检测 `NSExtension` 键直接 return（分享/动作/键盘 appex 无边缘返回需求，且 keyWindow 多为 null，避免无意义注入与日志噪声，如 `com.tencent.xin.sharetimeline`）。
- `99bbf8c` feat: 设置面板新增「返回灵敏度」分组，两个 `PSSliderCell`：① **提交位移阈值** `commitRatio`（default 0.3，min 0.1 / max 0.5）；② **提交速度阈值** `commitVelocity`（default 400，min 100 / max 1000）。用户可真机调跟手程度。defaults 域 `com.zlhkf.oback`。
- `ac5b269` feat: `ObackSettingsController` 实现 `slider:titleForMinimumValue:` / `titleForMaximumValue:` / `titleForCurrentValue:` 三个 delegate，让滑块两端显示最小/最大值、并**实时显示当前数值**（PSSliderCell 默认不显文字）。格式化：值 ≥1 按整数（速度），<1 保留两位小数（位移）。
- **主线现状**：`ac5b269`（已 push `main`，版本 `0.1.0+ac5b269`）。至此功能集：nav pop 视差+胶囊、modal dismiss 安全视差(方案B,只移 sheet)、灵敏度滑块真机可调、扩展进程跳过、AppTool 冲突退避（仅限 setDelegate 透传、手势不关）、提交阈值含惯性投影、triggerWidth=40、whitelistMode 默认 NO。
- **待真机验证（最终集）**：① 各 App 边缘内滑返回灵敏（弹性阈值+滑块可调）；② 弹窗边缘滑 dismiss 带 sheet 视差无黑屏；③ 同时装 AppTool 不进安全模式；④ 设置面板滑块数值/极值显示正常。

## 胶囊卡死修复（commit fce12dd，用户截图实锤）
- **现象**：用户截图显示 OPPO 风格胶囊箭头 `<` 卡在华为健康(`com.huawei.iossporthealth`)体温页面上无法消失。
- **根因**：`handlePan:` 的 switch 只处理 Began/Changed/Ended/Cancelled，**漏了 `UIGestureRecognizerStateFailed`**。而自定义 `ObackPanGestureRecognizer.touchesMoved:` 在纵向位移 > 横向时(前 8pt 判定)会主动设 `self.state = Failed`。事故链：边缘起滑 → shouldBegin=YES → **Began(胶囊显示)** → 带点纵向 → touchesMoved 设 **Failed** → handlePan 命中 default:break → **endTransition 从未调用 → 胶囊永远不清除**。
- **修复**：① switch 加 `UIGestureRecognizerStateFailed` 分支，走新的 `abortTransition:` 方法（dismissIndicatorCommitted:NO 取消弹回 + cancel 交互转场 + 重置 interacting/interactive/currentPercent）；② `showIndicatorWithEdge:` 清旧残留时先 `_indicator.layer.removeAllAnimations`(杀掉进行中的 dismiss 动画)再 removeFromSuperview，防连续手势时旧动画 completion 干扰新胶囊。
- **日志旁证**：oback_debug(5).log 第 167-172 行连续 6 次 shouldBegin=YES 无 beginTransition（Began 后立刻 Failed，但那次 Began 的胶囊没清）。所有 indicator shown 在日志里都有 endTransition——说明卡死发生在日志截取之后、或 Failed 发生在 Began→showIndicator 之后但下一次 shouldBegin 之前。

- **新现象（oback_debug(6).log）**：胶囊不卡屏了，但**界面冻结**——App 界面点不动、只能上滑回桌面；胶囊能正常划出。
- **新根因（冻结，28fc77b 根治）**：`ObackInteractiveTransition` 原是 `NSObject<UIViewControllerInteractiveTransitioning>`，手写 `startInteractiveTransition:` 存 `_ctx` + `finish/cancel` 直接 `[_ctx completeTransition:]`——绕过了 `UIPercentDrivenInteractiveTransition` 的 `finishInteractiveTransition/cancelInteractiveTransition`，导航控制器一直卡在"交互进行中"态 → 界面 userInteraction 失效（上滑是系统手势不受影响）。日志特征：每次 `shouldBegin=YES → beginTransition: pop nav → indicator shown → abortTransition(state=3) → interactive transition cancelled(finished=1)` 连发，且中间无 updateTransition（Began 即调用 popViewControllerAnimated:，手势稍动即取消）。
- **修复(28fc77b)**：① `ObackInteractiveTransition : UIPercentDrivenInteractiveTransition`，`updateWithPercent:`→`updateInteractiveTransition:`、`finish`→`finishInteractiveTransition`、`cancel`→`cancelInteractiveTransition`，删手写 `_ctx/fromView/toView/dimView/startInteractiveTransition:`（视差全交 `ObackAnimator.animateTransition:` 被 scrub）；② pop/dismiss 触发从 `beginTransition`(Began) 移到 `updateTransition` 首次 `p>0.001` 才调用（`triggerTransitionInWindow:`），纯点按/纵向不再启动交互转场。
- 待 CI 出 `0.1.0+28fc77b` 真机验证：界面不再冻结、各 App 边缘内滑返回灵敏、弹窗 dismiss 无黑屏。

- **CI 编译失败(510b361 修复)**：28fc77b 把 pop/dismiss 从 beginTransition 挪到 triggerTransitionInWindow: 后，`triggerTransitionInWindow:` 里残留 `ObackParams *p = [ObackPreferences params];` 未使用 → `-Werror,-Wunused-variable` 编译挂。删掉该行即过。教训：MRC tweak 工程开 `-Werror`，每删一处对变量的引用都要 grep 确认无残留。

- **新 bug（oback_debug(7).log）：左边缘胶囊不出现**。现象：左边缘有震动(haptic)、有效果(系统原生返回跑),但胶囊不出；右边缘正常。日志铁证：整份 `indicator shown` 全 `edge=右`,`edge=左` 一次都没出现（尽管多次 `shouldBegin=YES (左)`）。
- **根因**：iOS `UINavigationController.interactivePopGestureRecognizer` 是左边缘 `UIScreenEdgePanGestureRecognizer`,在左边缘抢赢我们的 pan → 我们的手势永远进不了 Began → 胶囊原只在 Began(`beginTransition:`)显示,故左边缘永不出胶囊；系统原生返回照常跑=用户感知的"效果"。
- **修复(4145117)**：① 胶囊显示从 `beginTransition:`(Began) 提前到 `gestureRecognizerShouldBegin:` 成功路径（与 haptic 同触发点,左右一致,贴合 OPPO"一碰边缘箭头就探出"）；② `showIndicator` 内加 0.4s 安全兜底 `dismissIndicatorSafety`（被抢走/未进入 Began 时自动收起,防残留）；③ `dismissIndicatorSafety` 用 `_indicator.window` 取真实 window 算回弹位置；④ `beginTransition`/`endTransition`/`abortTransition` 均 `cancelPreviousPerformRequests` 取消定时器。
