# Theos 构建产物（不需要提交）
.theos/
obj/
packages/
.deb.makeok
*.deb

# 系统 / 编辑器
.DS_Store
*.swp
*~
.idea/
.vscode/

# WorkBuddy 内部记忆（默认不进仓库，但项目记忆 memory/ 需跨电脑同步，单独放行）
.workbuddy/
!.workbuddy/memory/
!.workbuddy/memory/**
